##1.如何更改linux文件目录拥有者及用户组

>  chown  账号名称 文件或目录
>  chgrp  组名   文件或目录

##2.关机的步骤
1.who命令用来查看有没有其他用户在线
2.sync命令用来加快内存数据到磁盘的读写速度
3.shutdown命令

sudo命令:
sudo 允许一般用户使用 root 可执行的命令，不过只有在 /etc/sudoers 配置文件中添加的用户才能使用该指令。
