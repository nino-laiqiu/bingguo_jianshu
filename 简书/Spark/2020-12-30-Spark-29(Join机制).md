## hash join
场景:小表join
build table --小表 -- hash table [bucket] 类似于hashmap
缓存到内存放不小到磁盘
proble  table--大表 

使用相同的hash函数(在spark中相当于相同的partitioner),减少比较的次数

hash join性能如何:每张表只扫描一次
为什么要把小表加载到内存:显然,效率高
hash join是单机模式下的实现,
在分布式环境下,对其进行改造

## broadcast hash join
场景:极小表
条件:被广播的表小于10M  基类不能被广播

其中一张小表广播分发到另一张大表所在的分区节点上，分别并发地与其上的分区记录进行hash join

阶段:broadcast 一般有方案:driver统一把数据发给executor   p2p思路
        hashjoin  
## shuffler hash join
一旦小表数据量较大，此时就不再适合进行广播分发。这种情况下，可以根据join key相同必然分区相同的原理，将两张表分别按照join key进行重新组织分区，这样就可以将join分而治之，划分为很多小join，充分利用集群资源并行化

![image.png](https://upload-images.jianshu.io/upload_images/9049859-1aa083e43d92b2a8.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240)

步骤: 
1. 对两张表分别按照join keys进行重分区，即shuffle，目的是为了让有相同join keys值的记录分到对应的分区中
2. 对对应分区中的数据进行join，此处先将小表分区构造为一张hash表，然后根据大表分区中记录的join keys值拿出来进行匹配


##sort merge join
阶段:
1. 两张大表,key重新分区--shuffle阶段
2. 对每个分区的数据,分别排序--sort阶段
3. 归并思想:分别遍历两个相同hash分区的有序序列,碰到相同key就jion ,否则继续取值

![image.png](https://upload-images.jianshu.io/upload_images/9049859-7acf68e28a57f608.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240)





