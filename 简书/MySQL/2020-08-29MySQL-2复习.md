###1.MYSQL连接idea遇到的问题
解决导入数据库Server returns invalid timezone. Go to 'Advanced' tab and set 'serverTimezone' property manua
步骤如下:
set global time_zone = '+8:00';
show variables like 'time_zone';

###2.聚合查询
select 所要查询的每一个聚合函数或者字段是独立的互不影响的
聚合函数是对聚合之后形成的似临时表的操作

**where和having的区别**
  1.Where语句是一条一条从磁盘读取的, 然后进行判断,满足条件的存放到内存,不满足忽略,而having是将所有的数据读入内存中,然后在内存内部逐条判断,不满足直接删除(where是判断数据从磁盘读入内存的时候,having是判断分组统计之前的所有条件)
  2.having子句中可以使用字段别名，而where不能使用
  3.having能够使用统计函数，但是where不能使用
  4.where 后不能跟聚合函数，因为where执行顺序大于聚合函数。
  5.having 是筛选组  而where是筛选记录

desc 降序  asc 升序
**函数的执行顺序:from where group by  select having order by limit**
**过滤表中数据**
1.select distinct * 表名
2.select * from  表名 group by 所有字段 

遵循语法:
having 必须在group by 之后使用 而 有group by函数可以没有having
对于多表联查的规范:
如下 
```
/* (  (select 某个字段或者多个  from 表名  where .. ) as t1
  left join  
  (select 某个字段或者多个 from 表名 where ..) as t2  
   on   t1.  =  t2.  
   ) as t1
   left join  某个表  
   on  t1. =  某个表.
   ......三个表的联查采取这个写法 而不是嵌套结构
*/
```
