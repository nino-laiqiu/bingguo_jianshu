![image.png](https://upload-images.jianshu.io/upload_images/9049859-8e05bed426873fc4.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240)

```
with x as 
(
select 
Name, Salary,DepartmentId,
dense_rank() over(partition by DepartmentId  order by Salary desc)  as result
from 
Employee) 
select 
Department.Name AS Department ,
x.Name AS   Employee,
x.Salary AS Salary
from
Department join x on  x.DepartmentId = Department.Id  
where x.result = 1
```

![image.png](https://upload-images.jianshu.io/upload_images/9049859-5ff6f2fff486323b.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240)
```
select s.name as department,t.name as Employee,t.salary from (
select name,salary,departmentid,
dense_rank() over(partition by departmentid order by salary desc) as rn 
from 
Employee) t
join department s on t.departmentid = s.id 
where rn <=3
```
