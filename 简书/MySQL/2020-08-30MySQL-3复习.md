###1.子查询基本思路
###2.多表联查注意规范
###3.SQL的语法规范
```
select distinct Student.sid,Student.sname,score
from (Student
     left join SC S on Student.sid = S.sid
     )
where cid ='01' and score<60
order by score desc ;
```
和
```
select distinct Student.sid,Student.sname
from (Student
     left join SC S on Student.sid = S.sid
     )
where cid ='01' and score<60
order by score desc ;
```
和
```
select distinct Student.sid,Student.sname
from (Student
     left join SC S on Student.sid = S.sid
     )
where cid ='01' and score<60
group by Student.sid, Student.sname, score
order by score desc ;
```
第二 三个语法不规范......为什么

###4.日期函数
###5.作业
```
show databases ;
use test;
show tables;
create table tb_table1(
    useid int ,
    password varchar(10),
    username varchar(10)
);
select  * from tb_table1;
insert into tb_table1 values (1,'12345','王昭君'),(2,'123456','李白'),
                             (3,'123','貂蝉'),(4,'123456','董卓');
select * from tb_table1;
CREATE TABLE `products` (
                            `id` INT AUTO_INCREMENT,
                            `name` VARCHAR(40) ,
                            `price` DOUBLE ,
                            `category` VARCHAR(40) ,
                            `pnum` INT(11) ,
                            `description` VARCHAR(255) ,
                            PRIMARY KEY (`id`)
);
INSERT INTO `products` VALUES (NULL,'感悟',100,'励志',100,'一次心灵的鸡汤');
INSERT INTO `products` VALUES(NULL,'java与模式',67,'计算机',200,'让你的编程，从些不一样');
INSERT INTO `products` VALUES(NULL,'java并发编程实战',190,'计算机',49,'实战大于理论');
INSERT INTO `products` VALUES(NULL,'设计模式解析',88,'计算机',86,'头脑风暴');
INSERT INTO `products` VALUES(NULL,'搭地铁游上海',28,'生活百科',120,'一次不一样的旅行');
INSERT INTO `products` VALUES(NULL,'时空穿行',65,'科技',87,'这是一本好书');
INSERT INTO `products` VALUES(NULL,'中国国家地理',45,'生活百科',100,'了解你生活的国家');
INSERT INTO `products` VALUES(NULL,'欧洲', NULL,'生活',200,'你梦中向往的地方');
INSERT INTO `products` VALUES(NULL,'网管员必备宝典',35,'计算机',120,'上网新手必备书籍');
select  * from products;
select (price+10) as price from products;
select  ifnull(price,10) as price,id from products;
select products.category,max(price) as price1
from products
group by  products.category;

select products.name,products.category,price1,price
from
products left join (select products.category,max(price) as price1
from products
group by  products.category) as products1
on products.price = products1.price1
;
select products.category,max(price) as price1
from products
group by  products.category;

create table tb_table2(
    A int,
    B int,
    C int
);
insert into tb_table2 values (3,2,1),(3,2,2),(3,2,3),(3,2,4),(3,2,5),(3,2,6);
create table tb_table3(
       B int,
       C int
);
insert  into tb_table3 values (2,1),(2,2);
select  *
from tb_table2  left JOIN   tb_table3
on tb_table2.C=tb_table3.C;

create table Student(sid varchar(10),sname varchar(10),sage datetime,ssex nvarchar(10));
insert into Student values('01' , '赵雷' , '1990-01-01' , '男');
insert into Student values('02' , '钱电' , '1990-12-21' , '男');
insert into Student values('03' , '孙风' , '1990-05-20' , '男');
insert into Student values('04' , '李云' , '1990-08-06' , '男');
insert into Student values('05' , '周梅' , '1991-12-01' , '女');
insert into Student values('06' , '吴兰' , '1992-03-01' , '女');
insert into Student values('07' , '郑竹' , '1989-07-01' , '女');
insert into Student values('08' , '王菊' , '1990-01-20' , '女');
create table Course(cid varchar(10),cname varchar(10),tid varchar(10));
insert into Course values('01' , '语文' , '02');
insert into Course values('02' , '数学' , '01');
insert into Course values('03' , '英语' , '03');
create table Teacher(tid varchar(10),tname varchar(10));
insert into Teacher values('01' , '张三');
insert into Teacher values('02' , '李四');
insert into Teacher values('03' , '王五');
create table SC(sid varchar(10),cid varchar(10),score decimal(18,1));
insert into SC values('01' , '01' , 80);
insert into SC values('01' , '02' , 90);
insert into SC values('01' , '03' , 99);
insert into SC values('02' , '01' , 70);
insert into SC values('02' , '02' , 60);
insert into SC values('02' , '03' , 80);
insert into SC values('03' , '01' , 80);
insert into SC values('03' , '02' , 80);
insert into SC values('03' , '03' , 80);
insert into SC values('04' , '01' , 50);
insert into SC values('04' , '02' , 30);
insert into SC values('04' , '03' , 20);
insert into SC values('05' , '01' , 76);
insert into SC values('05' , '02' , 87);
insert into SC values('06' , '01' , 31);
insert into SC values('06' , '03' , 34);
insert into SC values('07' , '02' , 89);
insert into SC values('07' , '03' , 98);

select count(1)
from student
group by ssex;
/*1.查询“01”课程比“02”课程成绩高的所有学生的学号*/
select distinct t1.sid
from
     (select * from sc where cid ='01') as t1
left join
     (select *  from sc where cid ='02') as t2
on t1.sid = t2.sid
where  t1.score < t2.score;
/*2.查询平均成绩大于60分的同学的学号和平均成绩*/
select avg(score)as score1,sid
from sc
group by sid
having  score1>60;
/*3.查询所有同学的学号、姓名、选课数、总成绩*/
select count(cid),sum(score),S.sname,sc.sid,count(S.ssex)
from sc
left join Student S
on sc.sid = S.sid
group by  sc.sid,S.sname;

/*4.查询姓“李”的老师的个数；*/
select count(1)
from teacher
where teacher.tname like '李%';
/*5.查询没学过“张三”老师课的同学的学号、姓名；
  两表联查 这道题目重要*/
select Student.sid ,Student.sname
from student
where  sid not in
 ( select  s.sid  from teacher
     left join course c on Teacher.tid = c.tid
     left join sc s on c.cid = s.cid
     where  teacher.tname ='张三'
    );
/*方法二*  问题未解决/




select temp_tables.sname
from
     (select *
from
      teacher left join course c on teacher.tid = c.tid
      left join sc s on c.cid = s.cid
      left join student s2 on s.sid = s2.sid
      where  tname = '张三'
)
     as temp_tables;


select  * from teacher
                            left join course c on Teacher.tid = c.tid
                            left join sc s on c.cid = s.cid
                            left join student s2 on s.sid = s2.sid
;

/*6.查询学过“01”并且也学过编号“02”课程的同学的学号、姓名；*/



/*7.查询学过“张三”老师所教的课的同学的学号、姓名；*/
select sid,sname
from student
where sid
in
(
select s.sid
from  teacher left join Course C on Teacher.tid = C.tid
left join sc s on C.cid = s.cid
where  Teacher.tname like '张三');
/*8.查询课程编号“01”的成绩比课程编号“02”课程低的所有同学的学号、姓名；*/
select  sid,sname
from
     (
    select  t1.sid as sid
    from
           ( select * from sc where cid= '01') as t1
           left join
           (select * from sc where cid = '02') as t2
           on t1.sid = t2.sid
            where t1.score < t2.score )as t1
           left join
           student
           on t1.sid =student.sid;

/*9、查询所有课程成绩小于60分的同学的学号、姓名；*/
select Student.sid,sname
from student
left join SC S on Student.sid = S.sid
where  score<60
group by sid, sname;



/*10.查询没有学全所有课的同学的学号、姓名；*/
select  Student.sid , sname
from  student
left join SC S on Student.sid = S.sid
group by sid, sname
having  count(cid)<3;

/*11.查询至少有一门课与学号为“01”的同学所学相同的同学的学号和姓名；*/
select Student.sid,Student.sname
from Student
left join SC S on Student.sid = S.sid
where s.cid in (
select  s.cid
from
SC S
where S.sid='01')
group by Student.sid, Student.sname;

/*12.查询和"01"号的同学学习的课程完全相同的其他同学的学号和姓名
  ........复杂复杂复杂.........
  */


/*13.把“SC”表中“张三”老师教的课的成绩都更改为此课程的平均成绩；*/


/*14.查询没学过"张三"老师讲授的任一门课程的学生姓名*/

select sname
from student
where sid not in
      (
          select sid
          from sc
                   left join course c on SC.cid = c.cid
                   left join teacher t on c.tid = t.tid
          where tname = '张三'
      );
select
    sname
from student
where sid not in
      (
          select
              distinct sid
          from sc
                   left join course
                             on sc.cid=course.cid
                   left join teacher
                             on course.tid=teacher.tid
          where tname='张三'
      );


select  * from teacher
                   left join course  on teacher.tid  = course.tid
                  left join sc  on course.cid  = sc.cid
                   left join student on sc.sid  = student.sid ;

/*15、查询两门及其以上不及格课程的同学的学号，姓名及其平均成绩*/
select Student.sid,Student.sname
from (student
left join SC S on Student.sid = S.sid)
where score<60
group by Student.sid, Student.sname
having  count(score) >=2;

/*16.检索"01"课程分数小于60，按分数降序排列的学生信息*/
select  Student.sid,Student.sname,score
from (Student
     left join SC S on Student.sid = S.sid
     )
where cid ='01' and score<60
group by Student.sid, Student.sname, score
order by score desc ;
/*.......理解题意*/
select *
from sc
where cid = '01' and score < 60
order by score desc;


/*17.按平均成绩从高到低显示所有学生的平均成绩*/
select avg(sc.score)as p1
from sc
group by sc.sid
order by p1 asc ;

/*18、查询各科成绩最高分、最低分和平均分：以如下形式显示：
课程ID，课程name，最高分，最低分，平均分，及格率*

       格式.....
 */



/*19、按各科平均成绩从低到高和及格率的百分数从高到低顺序*/
/*20.查询学生的总成绩并进行排名*/
select  sum(score) p1
from sc
group by  sc.sid
order by p1 asc ;

select  sum(score) p1,Student.sid,Student.sname
from student
left join SC S on Student.sid = S.sid
group by  Student.sid, Student.sname
order by p1 asc
limit 1,7;


/*21.查询不同老师所教不同课程平均分从高到低显示
  多对多 可以匹配
  一对多  不能匹配  只能子查询获取对应的数值
  */
select avg(score) p1 ,c.cname,c.tid
from sc
left join course c on SC.cid = c.cid
left join teacher t on c.tid = t.tid
group by c.tid,c.cname
order by  p1 desc ;

select
    tid
     ,avg(score) as avg_score
from course
         left join sc
                   on course.cid=sc.cid
group by tid
order by avg_score desc;

/*22、查询所有课程的成绩第2名到第3名的学生信息及该课程成绩*/


/*23.统计各科成绩各分数段人数：课程编号,课程名称,
[100-85],[85-70],[70-60],[0-60]及所占百分比*/
/*24.查询学生平均成绩及其名次*/
select avg(ifnull(score,0)) p1
from student
left join SC S on Student.sid = S.sid
group by S.sid
order by p1;

/*25、查询各科成绩前三名的记录*/
/*26、查询每门课程被选修的学生数*/
select  count(cid),sc.sid
from sc
group by sid
;

/*27、查询出只选修了一门课程的全部学生的学号和姓名*/
select student.sid,student.sname
from student
left join SC S on Student.sid = S.sid
group by student.sid, student.sname
having count(cid) = 1;


/*28、查询男生、女生人数*/
select count(ssex),Student.ssex
from student
group by ssex;


/*29、查询名字中含有"风"字的学生信息*/
select student.sid,student.sname
from student
where Student.sname like '%风%'
group by student.sid, student.sname;


/*30、查询同名同性学生名单，并统计同名人数*/
select count(student.sname) p1 ,Student.sname
from student
group by  Student.sname
having p1>1;
```
