##1.总结
1) 高层模块不应该依赖低层模块，二者都应该依赖其抽象
2) **抽象不应该依赖细节，细节应该依赖抽象**
3) 依赖倒转(倒置)的中心思想是面向接口编程

##2.定义:
抽象不应该依赖于细节，细节应当依赖于抽象。换言之，要针对接口编程，而不是针对实现编程
依赖倒转原则是基于这样的设计理念：相对于细节的多变性，抽象的东西要稳定的多。以抽象为基础搭建的架构比以细节为基础的架构要稳定的多。在 java 中，抽象指的是接口或抽象类，细节就是具体的实现类

##3.代码说明:
未使用依赖倒转原则
```
public class Demo3 {
    public static void main(String[] args) {
        People people = new People();
        people.getMessage(new Email());
    }
}

class Email {
    public String send() {
        return "电子邮箱信息";
    }
}

//todo 假设不是电子邮箱呢?其他的信息源

class People {
    public void getMessage(Email email) {
        System.out.println(email.send());
    }
}
```
**借助一个接口**
```
public class Demo3 {
    public static void main(String[] args) {
        People people = new People();
        people.getMessage(new Email());
        people.getMessage(new Weixin());
    }
}

interface IReceiver {
    String send();
}

class Email implements IReceiver {

    @Override
    public String send() {
        return "电子邮箱信息";
    }
}

class Weixin implements IReceiver {

    @Override
    public String send() {
        return "微信的信息";
    }
}


class People {
    public void getMessage(IReceiver receiver) {
        System.out.println(receiver.send());
    }
}
```

##4.依赖倒转传递的三种方式

**接口传递**
```
public class Demo4 {
    public static void main(String[] args) {
        Pig pig = new Pig();
        pig.show(new Apple());
    }
}
interface Zoon {
    void show(Materials materials);
}

interface Materials {
    void play();
}

class Pig implements Zoon {

    @Override
    public void show(Materials materials) {
        materials.play();
    }
}

class Apple implements Materials {

    @Override
    public void play() {
        System.out.println("++++");
    }
}
```

**构造方法传递**
```
public class Demo4 {
    public static void main(String[] args) {
        Pig pig = new Pig(new Apple());
        pig.show();
    }
}

interface Zoon {
    void show();
}

interface Materials {
    void play();
}

class Pig implements Zoon {
    Materials materials;

    public Pig(Materials materials) {
        this.materials = materials;
    }

    @Override
    public void show() {
        this.materials.play();
    }
}

class Apple implements Materials {

    @Override
    public void play() {
        System.out.println("++++");
    }
}
```

**set方法**
```
public class Demo4 {
    public static void main(String[] args) {
        Pig pig = new Pig();
        pig.setmaterials(new Apple());
        pig.show();

    }
}

interface Zoon {
    void show();

    void setmaterials(Materials materials);
}

interface Materials {
    void play();

}

class Pig implements Zoon {
    Materials materials;

    @Override
    public void show() {
        materials.play();
    }

    @Override
    public void setmaterials(Materials materials) {
        this.materials = materials;
    }
}

class Apple implements Materials {

    @Override
    public void play() {
        System.out.println("++++");
    }
}
```
