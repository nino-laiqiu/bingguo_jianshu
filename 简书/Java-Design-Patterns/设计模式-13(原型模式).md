##1.浅拷贝 深拷贝
禁止使用clone来进行克隆,clone在实质上还是浅拷贝
(对象里包含对象的情况)
```
public class Test1 {
    public static void main(String[] args) throws CloneNotSupportedException {
        NA na = new NA("2");
        Student student = new Student("2",na);
        Student clone = (Student)student.clone();
        System.out.println(System.identityHashCode(student));
        System.out.println(System.identityHashCode(clone));
        System.out.println(student);
        System.out.println(clone);
        na.setAge("3");
        student.setAge("66");
        System.out.println("====");
        System.out.println(System.identityHashCode(student));
        System.out.println(System.identityHashCode(clone));
        System.out.println(student);
        System.out.println(clone);
    }
}

class Student{
    String age ;
    NA name;

    public Student(String age, NA name) {
        this.age = age;
        this.name = name;
    }

    public String getAge() {
        return age;
    }

    public void setAge(String age) {
        this.age = age;
    }

    @Override
    public String toString() {
        return "Student{" +
                "age='" + age + '\'' +
                ", name=" + name +
                '}';
    }

    @Override
    protected Object clone() throws CloneNotSupportedException {
        return new Student(this.age,this.name);
    }
}
class  NA{
     String age;

    public NA(String age) {
        this.age = age;
    }

    public String getAge() {
        return age;
    }

    public void setAge(String age) {
        this.age = age;
    }

    @Override
    public String toString() {
        return "NA{" +
                "age='" + age + '\'' +
                '}';
    }
}
```
```
381259350
2129789493
Student{age='2', name=NA{age='2'}}
Student{age='2', name=NA{age='2'}}
====
381259350
2129789493
Student{age='66', name=NA{age='3'}}
Student{age='2', name=NA{age='3'}}
```
##2.深拷贝
1.通过clone来克隆
2.序列化来克隆

##3.总结
缺点:
需要为每一个类配备一个克隆方法，而且该克隆方法位于一个类的内部，当对已有的类进行改造时，需要修改源代码，违背了“开闭原则”。
在实现深克隆时需要编写较为复杂的代码，而且当对象之间存在多重的嵌套引用时，为了实现深克隆，每一层对象对应的类都必须支持深克隆，实现起来可能会比较麻烦

创建新对象成本较大（如初始化需要占用较长的时间，占用太多的CPU资源或网络资源），新的对象可以通过原型模式对已有对象进行复制来获得，如果是相似对象，则可以对其成员变量稍作修改
