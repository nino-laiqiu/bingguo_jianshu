##1. nc和telnet命令

(1)telnet可以实现的功能：

连接服务器端口，并进行通信
登录远程telnet服务器，使用命令行对其进行控制

(2)nc可以实现的功能：

监听服务器端口，并与客户端通信（最多只能接收一个客户端）
对指定服务器进行端口扫描
作为客户端连接到远程服务器进行通信

## 2.nc -lk 和nc -lp  的区别是什么

* -l 开启 监听模式，用于指定nc将处于监听模式。通常 这样代表着为一个 服务等待客户端来链接指定的端口。
* -p<通信端口> 设置本地主机使用的通信端口。有可能会关闭
* -k<通信端口>强制 nc 待命链接.当客户端从服务端断开连接后，过一段时间服务端也会停止监听。 但通过选项 -k 我们可以强制服务器保持连接并继续监听端口。

##3. tail -f 与tail -F tailf 区别(监听相当于服务器)

* 等同于–follow=descriptor 根据文件描述符进行追踪，当文件改名后删除后，停止追踪

* 等同于–follow=name–retry 根据文件名进行追踪，并保持重试，即使该文件被删除或改名后，如果再次创建相同的文件名，会继续追踪

* 等同于tail -f n 10 (默认tail -f或者-F 默认也是打印最后10行，然后追踪文件)，与tail -f 不同的是，如果文件增长，他不会去访问磁盘，所以tailf 特别适合那些便携机上跟踪日志文件，因为他减少了磁盘访问，可以省电

常用操作：
* 此时要想暂停刷新，使用【Ctrl】+ 【S】暂停终端
* 若想继续终端，使用【ctrl】+【q】
* 若想退出tail命令 直接使用【ctrl】+【c】

##4.nohup用法
不挂断的运行命令
(其他说明)

##5.flume启动命令的说明

##6.find命令的学习
* find /home  -name "*.t?t"   *表示通配任意个字符  ？表示通配单个字符


* find   /usr   -mtime  -4   查找文件更新日时在距现在时刻4天以内的文件
*  find   /usr  -mtime  +4    查找文件更新日时在距现在时刻5天以上的文件
*  find   /usr   -mtime   4   查找文件更新日时在距现在时刻4天以上5天以内的文件


* find  / -user mhy  按照文件所有者和群组来查找：


* find  / -size  +10M 按照大小来查询



##7.说明什么是业务系统

#38.avro序列化的特点
avro是一个序列化框架,与java的相比它跨平台(多语言)
