1.单链表
```
public class Demo2 {
    public static void main(String[] args) {

    }
}
class List{
    //这是一个头节点不能动
    private  Node head = new Node("",0);
    //
    //添加尾部
   public  void  add(Node newnode){
       Node temp =head;
       while (true){
           if (temp.next ==null){
               break;
           }
           else {temp =temp.next;}
       }
       //当while循环跳出,则找到了最后的节点
       temp.next = newnode;

   }
   //遍历链表
    public  void list(){
       //先判断链表是否为空
        if (head.next ==null){
            System.out.println("链表为空");
            return;
        }
        Node temp =head.next;
        while (true){
            //判断是否到尾部了
            if (temp == null){
                System.out.println("遍历到尾部了");
                break;
            }
            //打印信息
            System.out.println(temp);
            temp = temp.next;
        }


    }
    //按指定位置 插入
    //新节点.next = temp.next
    //temp.next = 新节点
public  void  add2(Node node){
       boolean folg =false;//用来判断是否有重复的
    Node temp = head;
    while (true) {
        if (temp.next == null) {
            System.out.println("最后了");
            break;
        } else if (temp.next.age > node.age) {
            System.out.println("找到了");
            break;
        } else if (temp.next.age == node.age) {
            System.out.println("已经存在了");
            folg = true;//
            break;
        }
        ////这是一个核心
        temp = temp.next;//后移
    }
        if (folg ==true){
            System.out.println("已经存在");
        }
        else {
            node.next = temp.next;
            temp.next =node;
        }
    }

//修改节点的信息update
    public  void update(Node newnode){
       Node temp = head.next;
       boolean golg = false;
       while (true){
       if (temp ==null) {
           System.out.println("最后一个了");
           break;
       }
       else if (temp.age == newnode.age){
           golg =true;
           System.out.println("找到了");
           break;
       }
       temp =temp.next;
       }
       if (golg){
           System.out.println("找到了 更新");
           temp.name =newnode.name;
       }
       else {
           System.out.println("没有找到");
       }
    }
    //删除
    public  void  del(int age){
       Node temp =head;
       boolean flog =false;//判断是否找到了
       while (true){
       if (temp.next ==null) {
           System.out.println("已经遍历到最后了");
           break;
       }
       else if (temp.next.age ==age){
           System.out.println("找到了");
           flog =true;
           break;
       }
       temp =temp.next;
       }
       if (flog){
           System.out.println("找到了");
           //核心
           temp.next =temp.next.next;
       }
       else {
           System.out.println("没有找到");
       }
    }
}
//节点
class  Node{
    String name;
    int age;
    Node next;

    @Override
    public String toString() {
        return "Node{" +
                "name='" + name + '\'' +
                ", age='" + age + '\'' +
                '}';
    }

    public Node(String name, int age) {
        this.name = name;
        this.age = age;
    }
}
```
2.二叉树
![image.png](https://upload-images.jianshu.io/upload_images/9049859-b4152d0880e0a067.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240)
##二叉树的三种遍历
```
public class Demo4 {
    public static void main(String[] args) {
        //创建一颗二叉树吧
        BinaryTree binaryTree = new BinaryTree();
        //创建需要的节点
        Hode root = new Hode(1, "11");
        Hode hode2 = new Hode(2, "22");
        Hode hode3 = new Hode(3, "33");
        Hode hode4= new Hode(4, "44");
        //手动创建该二叉树
        root.setLeft(hode2);
        root.setRight(hode3);
        hode2.setLeft(hode4);
        binaryTree.setRoot(root);
        //测试
    }

}
class  BinaryTree{
    private  Hode root;

    public void setRoot(Hode root) {
        this.root = root;
    }
    //前
    public void preOrder(){ if(this.root!=null){ this.root.preOrder(); }else{ System.out.println("二叉树为空，无法遍历"); } }
    //中
    public void infixOrder(){ if(this.root!=null){ this.root.infixOrder(); }else{ System.out.println("二叉树为空，无法遍历"); } }
    //后
    public void postOrder(){ if(this.root!=null){ this.root.postOrder(); }else{ System.out.println("二叉树为空，无法遍历"); } }


}

    class Hode {
    private int no;
    private String name;
    private Hode left;
    private Hode right;

    //前序遍历
    public void preOrder() {
        System.out.println(this);
        if (this.left != null) {
            this.left.preOrder();
        }
        if (this.right != null) {
            this.right.preOrder();
        }
    }

    //中序遍历
    public void infixOrder() {
        if (this.left != null) {
            this.left.infixOrder();
        }
        System.out.println(this);
        if (this.right != null) {
            this.right.infixOrder();
        }
    }

    //后序遍历
    public void postOrder() {
        if (this.left != null) {
            this.left.postOrder();
        }
        if (this.right != null) {
            this.right.postOrder();
        }
        System.out.println(this);
    }


    public int getNo() {
        return no;
    }

    public void setNo(int no) {
        this.no = no;
    }

    public String getName() {
        return name;
    }

    public void setName(String name) {
        this.name = name;
    }

    public Hode getLeft() {
        return left;
    }

    public void setLeft(Hode left) {
        this.left = left;
    }

    public Hode getRight() {
        return right;
    }

    public void setRight(Hode right) {
        this.right = right;
    }

    public Hode(int no, String name) {
        this.no = no;
        this.name = name;
    }

    @Override
    public String toString() {
        return "Hode{" +
                "no=" + no +
                ", name='" + name + '\'' +
                '}';
    }

    public Hode() {
    }
}
```
