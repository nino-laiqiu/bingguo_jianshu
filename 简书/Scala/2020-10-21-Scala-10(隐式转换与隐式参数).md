#####1.案例(implicit)
要点:隐式转换的匹配是唯一的
```
   implicit  def  fun(d:Double) = d.toInt
       var age :Int=3.8
        println(age)
```
#####2.动态地增加类的功能
```
object Test3 {
  def main(args: Array[String]): Unit = {
    val a = new A
    a.funA()
    a.funB
  }
  implicit  def  funA1(a:A):B={
       new B
  }
}

class A {
  def funA(): Unit = {
    println("funA")
  }
}
class B{
    def funB:Unit={
         println("funB")
    }
}
```

#####3.隐式值

![image.png](https://upload-images.jianshu.io/upload_images/9049859-f432733ca8e4f1a3.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240)

![image.png](https://upload-images.jianshu.io/upload_images/9049859-553ad472493ee8f6.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240)

#####4.隐式类
![image.png](https://upload-images.jianshu.io/upload_images/9049859-9661d3eabc1a3e0a.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240)
```
//隐式类
object Tset4 {
  def main(args: Array[String]): Unit = {
    implicit class DB1(var a: Mysql) {
      def addFun(): Unit = {
        println( "name")
      }
    }
    implicit def mysql1(db: Mysql): DB = {
      new DB
    }

    val mysql = new Mysql
    mysql.myFun()
    mysql.addFun()
    mysql.add()


  }
}

class Mysql {
  def myFun(): Unit = {
    println("====")
  }
}

class DB {
  def add(): Unit = {
    println("----")
  }
}
```

#####5.隐式转换的时机

当方法中的参数的类型与目标类型不一致时, 或者是赋值时。
当对象调用所在类中不存在的方法或成员时，编译器会自动将对象进行隐式转换(根据类型)

######6.隐式解析机制(即编译器是如何查找到缺失信息的):

首先会在当前代码作用域下查找隐式实体(隐式方法、隐式类、隐式对象)。(一般是这种情况
如果第一条规则查找隐式实体失败，会继续在隐式参数的类型的作用域里查找。类型的作用域
是指与该类型相关联的全部伴生模块，一个隐式实体的类型 T 它的查找范围如下(第二种情况范围广且 复杂在使用时，应当尽量避免出现):
如果 T 被定义为 T with A with B with C,那么 A,B,C 都是 T 的部分，在 T 的隐式解析过程中， 它们的伴生对象都会被搜索。
如果 T 是参数化类型，那么类型参数和与类型参数相关联的部分都算作 T 的部分，比如 List[String]的隐式搜索会搜索 List 的伴生对象和 String 的伴生对象。
如果 T 是一个单例类型 p.T，即 T 是属于某个 p 对象内，那么这个 p 对象也会被搜索。
如果 T 是个类型注入 S#T，那么 S 和 T 都会被搜索
