##1.Set
#####java中的hashset
![image.png](https://upload-images.jianshu.io/upload_images/9049859-1da65a4ba7baad65.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240)
![image.png](https://upload-images.jianshu.io/upload_images/9049859-38fcff8e999314b3.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240)
##2.案例

#####Wordcount案例
```object Test1 {
  def main(args: Array[String]): Unit = {
    //wordcount案例
    //统计单词出现的次数
    var list = List("scala java php c++", "scala java c# c")
    var result = list.flatMap(_.split(" "))
      .map((_, 1))
      .groupBy(_._1)
      //这里为什么能用元组的_1 _2....输入的就是元组?????
      .map(data => (data._1, data._2.size))
      .toList
      .sortBy(_._1)
    println(result) //List((c,1), (c#,1), (c++,1), (java,2), (php,1), (scala,2))
    //不能这么写,以前的案例是读取一行,按照行来切割
    var result0 = list.map(data => {
      var txt = data.split(" ")
      (txt,1)
    })

    var result1 = list.flatMap(_.split(" "))
                      .map((_,1))
                      //list集合用不来keyby???
    println(result1)
  }
}
```

#####对于操作符的补充

4种操作符的区别和联系
(1) :: 该方法被称为cons，意为构造，向队列的头部追加数据，创造新的列表。用法为 x::list,其中x为加入到头部的元素，无论x是列表与否，它都只将成为新生成列表的第一个元素，也就是说新生成的列表长度为list的长度＋1(btw, x::list等价于list.::(x))
(2) :+和+: 两者的区别在于:+方法用于在尾部追加元素，+:方法用于在头部追加元素，和::很类似，但是::可以用于pattern match ，而+:则不行. 关于+:和:+,只要记住冒号永远靠近集合类型就OK了。
(3) ++ 该方法用于连接两个集合，list1++list2
(4) ::: 该方法只能用于连接两个List类型的集合

#####模式匹配
```
object Test2 {
  def main(args: Array[String]): Unit = {
    //模式匹配案例
    //1.给一个元组交换元素的位置
    println(stap((1, 2)))
    //2.交换数组前两个元素的位置,要求数组的长度大于二
    println(stap1(Array(1, 2, "90", true)).toBuffer)
    //3.求和,some集合,给非空默认值
    var list = List(Some(1), None, Some(12), None, Some(8))
    println(sum(list))
    //可以这样求和??????
    var list1 = List(1, 2, 3)

    println(list1.sum)//6

  }

  //泛型的写法
  def stap[T, S](tuple: (T, S)) = {
    tuple match {
      case (a, b) => (b, a)
      case _ =>
    }
  }

  def stap1(array: Array[Any]): Array[Any] = {
    array match {
      //Array(second, first, last)  Array(second, first)  :+last 不能这样ArrayBuffer(2, 1, ArraySeq(90, true))
      case Array(first, second, last@_*) => Array(second, first) ++ last

    }
  }
  //option集合作用防止空指针......!!!!!
  def sum(list: List[Option[Int]]) = list.map(x => x.getOrElse(0)).sum //求和
}
```
