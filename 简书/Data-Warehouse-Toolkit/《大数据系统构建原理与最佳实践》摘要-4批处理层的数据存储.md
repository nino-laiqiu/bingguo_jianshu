  **一次写入,多次批量读取**

##批处理层主数据集存储的需求代码清单                                               

写:

高效追加新数据

可扩展的存储

读:

支持并行处理

读写:

可调优存储和处理成本

强制不变性



##为批处理层选择存储方案

**(键/值)-分布式**

什么是值--需要储存的数据
什么是键--生成一个通用的唯一识别码作为键

文件和文件夹的抽象-改善主数据集的储存

##分布式文件系统

##垂直分区!!!--->理解为一种存储标准
**问题是这是否要生成一个副本,还是直接以此作为主数据集**

函数只会访问到与计算相关的数据,这个过程被称为垂直分区

例如:为每天的的数据创建一个独立的文件夹

##分布式文件系统的底层性质

重名问题
文件格式不同
存储成本和处理成本的矛盾  `
