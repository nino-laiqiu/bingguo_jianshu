>本文的内容来自数据之路
阅读并记录一些知识点和流程


##1.日志采集
**基本的统计指标 PV UV**

>问题的描述:
网页页面访问的流程
交互日志如何采集
对恶意流量日志进行合法性检验
面对不同事件的日志数据应该做出怎么样的采集手段
日志统计如何降低复杂性,成本又能较为准确的反应出行为事件,呈现有价值的信息
日志数据的结构化和规范化

##2.数据同步

#####传统的同步方法
直连同步
JDBC/ODBC统一标准接口
如果数据量过大,采用此种抽取方式性能较差,不太适合从业务系统到数据仓库系统

数据文件同步
文件服务器

数据库日志解析同步
数据延迟
投入较大
数据漂移和遗漏

#####批量数据同步
dataX  sqoop

####实时数据同步
解析数据库的binlog日志/归档日志
同步方法:日志类型多种多样,设置主题分类同步,体现数据治理

#####增量和全量同步合并
在传统的数据整合方案中,大多采用merge(update insert ),但是当前主流的大数据平台不支持update操作,所以使用全外连接和数据覆盖全量加载(insert overwrite)

####同步性能的处理
**数据同步遇到的问题**
1.有些数据同步任务的总线程数达不到用户设置的首轮同步的线程数时,如果同步控制器将这些同步线程分发到CPU比较繁忙的机器上,将导致这些同步任务的平均同步速度非常低,数据同步速度非常慢。
2.用户不清楚该如何设置首轮同步的线程数,基本都会设置成一个固定的值,导致同步任务因得不到合理的CPU资源而影响同步效率。
3.不同的数据同步任务的重要程度是不一样的,但是同步控制器平等对待接收到的同步线程,导致重要的同步线程因得不到CPU资源而无法同步。

**负载均衡的同步方案**
>1.用户创建数据同步任务,并提交该同步任务。
2.根据系统提前获知及设定的数据,估算该同步任务需要同步的数据量、平均同步速度、首轮运行期望的线程数、需要同步的总线程数。
3.根据需要同步的总线程数将待同步的数据拆分成相等数量的数据块,一个线程处理一个数据块,并将该任务对应的所有线程提交至同步控制器。同步控制器判断需要同步的总线程数是否大于首轮运行期望的线程数,若大于,则跳转至;若不大于,则跳转至。
4.同步控制器采用多机多线程的数据同步模式,准备该任务第一轮线程的调度,优先发送等待时间最长、优先级最高且同一任务的线程。
5.同步控制器准备一定数据量(期望首轮线程数-总线程数)的虚拟线程,采用单机多线程的数据同步模式,准备该任务相应实体线程和虚拟线程的调度,优先发送等待时间最长、优先级最高且单机CPU剩余资源可以支持首轮所有线程数且同一任务的线程,如果没有满足条件的机器,则选择CPU剩余资源最多的机器进行首轮发送。
6.数据任务开始同步,并等待完成。
7.数据任务同步结束。

**数据漂移的解决方案**

**时间戳:**
数据库标识数据记录更新时间
问题:可能发生了实际的业务,但是数据并没有更新或者在第二天才更新

数据库日志用来标识数据记录更新时间
问题:如果数据量过大的情况下,可以日志的记录时间会大于实际业务发生的时间,造成数据漂移

数据库表中用来记录具体业务过程的发生时间
问题:只是包含了一个业务,事实上这个业务有更多的细节

标识数据记录的抽取时间
抽取需要时间,所以时间往往晚于前三个时间

**解决方案**
1.多记录一天
2.设计多记录第二天的凌晨前多少分钟时间



