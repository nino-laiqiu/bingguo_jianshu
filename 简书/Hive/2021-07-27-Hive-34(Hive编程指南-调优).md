##1.使用EXPLAIN
打印出抽象语法树。它表明Hive是如何将查询解析成token(符号)和literal(字面值)的,是第一步将查询转化到最终结果的一部分
##2. EXPLAIN EXTENDED
没用过学习一下,解释器和分词器.....

##3. 限制调整
LIMIT语句是大家经常使用到的,经常使用CLI的用户都会使用到。不过,在很多情况下LIMIT语句还是需要执行整个查询语句,然后再返回部分结果的。因为这种情况通常是浪费的,所以应该尽可能地避免出现这种情况。Hive有一个配置属性可以开启,当使用LIMTI语句时,其可以对源数据进行抽样
```
hive.limit.optimize.enable = true
```
```
hive.limit.row.max.size

hive.limit.optimize.limit.file
```
![参数的设置](https://upload-images.jianshu.io/upload_images/9049859-416b872d0c68478c.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240)

##4. JOIN 优化
##5. 本地模式
适用于输入数据量较小的情况
##6. 并行执行
通过设置参数`hive.exec.parallel值为true`,就可以开启并发执行
##7. 严格模式
通过设置属性`hive.mapred.mode值为strict`可以禁止3种类型的查询。

1. 对于分区表,where后不加分区字段
2. order by后不加limit
3. 笛卡尔积
##8. 调整mapper和reducer的数量
hive是根据输入数据的量来确定reduce的数量的
属性`hive.exec.reducers.bytes.per.reducer的默认值是1GB`

当在共享集群上处理大任务时,为了控制资源利用情况,属性hive.exec.reducers.max显得非常重要。一个Hadoop集群可以提供的map和reduce资源个数(也称为“插槽”),是固定的。某个大job可能就会消耗完所有的插槽,从而导致其他job无法执行。hive.exec.reducers.max可以阻止某个查询消耗太多的reducer资源,计算公式为:(集群总Reduce槽位个数*1.5) / (执行中的查询的平均个数)

##9. JVM重用
JVM重用是Hadoop调优参数的内容,其对Hive的性能具有非常大的影响,特别是对于很难避免小文件的场景或task特别多的场景,这类场景大多数执行时间都很短。
开启JVM重用将会一直占用使用到的task插槽,以便进行重用,直到任务完成后才能释放。如果某个“不平衡的”的job中有某几个reduce task执行的时间要比其他reduce task消耗的时间多得多的话,那么保留的插槽就会一直空闲着却无法被其他的job使用,直到所有的task都结束了才会释放。

##10. 索引
bitmap

##11. 动态分区调整
对动态分区插入的简单优化:生成分区最大值的限制
##12. 推测执行
空间换时间
关于调优这些推测执行变量,还很难给一个具体的建议。如果用户对于运行时的偏差,非常敏感的话,那么可以将这些功能关闭掉。如果用户因为输入数据量很大而需要执行长时间的map或者reduce task的话,那么启动推测执行造成的浪费是非常巨大的。
##13. 单个MapReduce中多个group by
##14. 虚拟列

主要的知识:
1. 学习使用EXPLAIN
2. 如何设置优雅的map和reduce数量
3. 关于优化这个模块.我发现市面上的几乎所以的hive优化资料都来源于此hive实践这本书...
