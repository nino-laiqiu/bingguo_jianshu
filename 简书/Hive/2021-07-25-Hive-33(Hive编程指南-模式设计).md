##1. 按天划分的表
反模式
##2. 分区表
hive通常情况下要扫描全表,这时候分区表就非常有用,但是分区过多,会导致文件过多,元数据过多,会造成namenode内存不足的问题(每个文件大致是150字节)
与此同时,MapkReduce会将一个任务(job)转换成多个任务(task),默认情况下,每个task都是一个新的JVM实例,都需要开启和销段的开销。对于小文件,每个文件都会对应一个task在一些情况下, JVM开启和销毁的时间中销毁可能会比实际处理数据的时间消耗要长!
因此,一个理想的分区方案不应该导致产生太多的分区和文件夹目录,并且每个目录下的文件应该足够得大,应该是文件系统中块大小的若干倍。
一个坏的分区划分规则,可能会导致某些分区过大,如果没有适合的分区方法,可参考分桶表来解决问题
##3. 唯一键和标准化
非标准化结构表达逻辑结构关系
##4.同一份数据多种处理
![改进](https://upload-images.jianshu.io/upload_images/9049859-7c79bdb4982f083e.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240)
??hive支持这种写法,没写过,这样写只要扫描一次表就行了
##5. 对于每个表的分区
数据同步问题
读取上游的数据,如果上游数据在发生覆盖重写的操作,就会有读取错误的数据的可能性,比较好的方法就是在覆盖重写之前就删除老的分区
##6. 分桶表数据存储
分区提供一个隔离数据和优化查询的便利的方式。不过,并非所有的数据集都可形成合理的分区,特别是之前所提到过的要确定合适的划分大小这个疑虑。
##7. 为表增加列
```sql
ALTER TABLE weblogs ADD COLUMNS (user id string);
```
##8. 使用列存储表
什么是列式存储
##9. 总是使用压缩
压缩和解压缩都会消耗CPU资源。MapReduce任务往往是1/O密集型的,因此CPU开销通常不是问题。不过,对于工作流这样的CPU密集型的场景,例如一些机器学习算法,压缩实际上可能会从更多必要的操作中获取宝贵的CPU资源,从而降低性能。

主要的知识:
1. 标准化和非标准化的舍取
2. 分桶表的正确使用
3. 什么是列式存储以及其优点
4. 各种压缩格式的使用优点
