##1. hive的必要性
大数据迁移到hadoop,hive的出现方便了大量的SQL用户
##2. 数据类型和数据编码
#####1. 基本数据类型
#####2. 集合数据类型
`struct ` `map ` `array`
大多数的关系型数据库不支持这个集合数据类型,它们趋向于破坏标准格式,在大数据系统中,不遵循标准格式的一个好处就是可以提供更高吞吐量的数据。
#####3. 文本文件数据编码
![hive中默认的记录和字段分隔符](https://upload-images.jianshu.io/upload_images/9049859-7530853b7b0f23e8.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240)
##3. 数据定义
1. 创建数据库的基本语法
2. 修改表方法

关于这个部分还是可以规范一下的,平时工作的时候就是直接按照规范来建表以及新增字段,并没有实际数据定义使用规范

##4. 数据操作
1. 动态分区插入数据
2. 其他插入表数据的方法

##5. 查询操作
#####1. 使用函数
`数学函数`
`聚合函数`
不支持在同一个select出现多个distinct
`表生成函数`
行转列函数
`其他内置函数`
#####2. 关于浮点数的比较
这是一个重要的细节问题
#####3. 语法
`limit`
`group by`
`join `
`union all`
值得注意的用法区别:SORT BY,DISTRIBUTE BY
>https://www.cnblogs.com/yjd_hycf_space/p/11140556.html

`ORDER BY / SORT BY`
`含有SORT BY的DISTRIBUTE BY`
`CLUSTER BY`
##6. 视图
使用视图的优势:降低查询的复杂度
##7. 模式设计
标准化和非标准化的舍取
数据同步问题:首先删除旧的分区
分区表的使用:如何设计出好用的分区表
分桶表的使用
什么是列式存储,各种压缩格式的使用优点
##8. 调优
这块内容是重中之重
#####1. EXPLAIN
抽象语法树,学习看懂分词器和解释器
#####2. join优化
#####3. 严格模式
#####4. 小文件问题
#####5. shuffle的问题:如何设置优雅的map和reduce数量
##9. 压缩方法
未曾看过的知识
job的可分隔性和不可分割性:大文件是如何切分的,如何读取的?
#####1. 中间结果压缩
#####2. 存档分区
HAR,减少node内r存的压力,但是实际上小文件还是没有减少

##10. 函数
##### 1. 标准函数(UDF)
##### 2. 聚合函数(UDAF)
##### 3. 表生成函数(UDTF)
#####4. 用户自定义函数

##11. Thrift服务
#####1. 启动Thrift Server
后台启动 `bin/hive --service hiveserver &`
检查HiveServer是否启动成功的最快捷方法就是使用netstat命令查看10, 000端口是否打开并监听连接`netstat -nl I grep 10000`
##12. 安全/锁
