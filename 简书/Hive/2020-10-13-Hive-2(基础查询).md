#####1.对ROW FORMAT DELIMITED FIELDS  TERMINATED  BY '\t';的说明
row 行
format 格式化
delimited 划分界限
fields 字段
terminated 终止

##2.hive有哪些保存元数据的方式，各有什么特点

#####3.hive日志
![image.png](https://upload-images.jianshu.io/upload_images/9049859-63d11109ed817150.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240)

#####4.排序
1.order by  全局排序只有一个reduce
2.sort by  reduce内部排序

> set mapreduce.job.reduces=3; (设置reduce的数目)

3.distribute by  指定分区字段 map

4. cluster by  当distribute by和sorts by字段相同时，可以使用cluster by方式。

#####5.分桶表
case when 语法:
```
CASE 字段 
 
WHEN 值1 THEN 值1
 
[WHEN 值2 THEN 值2]*
 
[ELSE 值] 
 
END 
```

```
CASE 
 
WHEN 条件表达式 THEN 值1
 
[WHEN 条件表达式 [and or] 条件表达式THEN 值2]*
 
[ELSE 值] 
 
END 
```

with 语法:
```
WITH 
t1 as (SELECT * FROM Table1) ,
t2 as (SELECT * FROM Table2) 
SELECT t1.*
FROM t1
LEFT JOIN t2 ON t1.id=t2.id ;
```

#####6.常用函数



#####hive启动报错(hive元数据启动)

>Exception in thread "main" java.lang.RuntimeException: java.lang.RuntimeException: Unable to instantiate org.apache.hadoop.hive.ql.metadata.SessionHiveMetaStoreClient

>https://blog.csdn.net/qq_20679251/article/details/81952941?ops_request_misc=%257B%2522request%255Fid%2522%253A%2522160272568519724839230044%2522%252C%2522scm%2522%253A%252220140713.130102334..%2522%257D&request_id=160272568519724839230044&biz_id=0&utm_medium=distribute.pc_search_result.none-task-blog-2~

#####7.类型的转换

Hive的原子数据类型是可以进行隐式转换的，类似于Java的类型转换，例如某表达式使用INT类型，TINYINT会自动转换为INT类型，但是Hive不会进行反向转化，例如，某表达式使用TINYINT类型，INT不会自动转换为TINYINT类型，它会返回错误，除非使用CAST操作。

#####8.数据类型


#####9.一个例题
```
create table gz
(
    uid    int,
    jb     int,
    jj     int,
    tc     int,
    deptno int
);
insert into gz
values (1, 2000, 3000, 1500, 1);
insert into gz
values (2, 5000, 500, 1000, 2);
insert into gz
values (3, 1500, 1000, 3000, 2);
insert into gz
values (4, 3000, 6000, 8000, 3);
insert into gz
values (5, 1500, 2000, 1800, 1);
insert into gz
values (6, 2500, 1000, 1900, 1);
create table bm
(
    deptno varchar(10),
    name   varchar(10)
);
insert into bm
values ('1', '销售');
insert into bm
values ('2', '技术');
insert into bm
values ('3', '行政');
create table yg
(
    uid    int,
    name   varchar(10),
    gender varchar(10),
    age    int
);
insert into yg
values (1, 'zs', 'M', 28);
insert into yg
values (2, 'ww', 'F', 48);
insert into yg
values (3, 'zs', 'F', 44);
insert into yg
values (4, 'zs', 'M', 32);
insert into yg
values (5, 'zs', 'M', 32);

#求出公司中每个员工的姓名 和 三类收入中最高的那种收入的类型
#求出公司中每个岗位的薪资总和
# 求出公司中每个岗位不同性别员工薪资总和
# 求出公司中不同性别、不同年龄阶段（20-30,31-40,41-50）的员工薪资总和
with x as (
select
t1.name,
t2.jb,
t2.jj,
t2.tc,
greatest(t2.jb,t2.jj,t2.tc) as great_max,
#case when
case  greatest(t2.jb,t2.jj,t2.tc)
when  t2.jb  then 'jb'
when  t2.jj  then  'jj'
when t2.tc then  'tc'
end  t3
from
yg t1
join gz t2  on t1.uid =t2.uid)
select x.name, x.t3
from  x;

select
sum(t1.jb+t1.jj+t1.tc),
case
t1.deptno
when  '1' then '销售'
when  '2' then '技术'
when  '3' then '行政'
end  deptno
from gz t1
join bm t2 on t1.deptno = t2.deptno
group by t1.deptno;

# 求出公司中每个岗位不同性别员工薪资总和
# 如果一个部门存在男女.....
with x as (
select t3.gender,t1.deptno,
sum(t1.jb+ t1.jj+t1.tc) sum_salary
from
gz t1
# join bm t2 on t1.deptno = t2.deptno
join  yg t3 on t1.uid = t3.uid
group by t3.gender,t1.deptno)
select x.deptno,x.gender,x.sum_salary,t4.name
from x
join  bm  t4 on x.deptno =  t4.deptno;

# 求出公司中不同性别、不同年龄阶段（20-30,31-40,41-50）的员工薪资总和
with  x as (
select
case
when t2.age >20 and t2.age <30  then  '20-30'
when t2.age >31 and t2.age <40  then  '31-40'
when t2.age >41 and t2.age <50  then  '41-50'
end age_year,
t2.gender,t2.uid
from
yg t2 )
select  sum(t3.tc+t3.jj+t3.jb),x.gender , age_year
from x
join gz t3 on  x.uid= t3.uid
group by  x.gender , age_year ;

```





