#####1. 去重的对象
由去重的对象不同导致的去重错误,有次需求要求去重,分别使用distinct 和 group by 两种方法去重,发现我去重的对象不同,导致了结果的不同


#####2. 去重的时间
有个需求本来是限制一天的去重,现在改写为多天的去重,然后我发现直接限制d时间区间的范围和直接group by d之后获取得到的结果不同,原因是区间去重,是这个区间的全部数据进行去重,而group by d 则是按照一天的维度来去重
