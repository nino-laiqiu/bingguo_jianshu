##1. SELECT ...... FROM ......语法
#####如何引用集合数据类型的元素
```
ARRAY      object[0]
MAP        object["element"]
STRUCT     object.element
```

##2. 使用正则表达式来指定列
##3. 使用列值进行计算
##4. 算术运算符
用户需要注意所使用的数值数据的数值范围,并确认实际数据是否接近表模式中定义的数据类型所规定的数值范围上限或者下限,还需要确认人们可能对这些数据进行什么类型的计算。
##5. 使用函数
#####数学函数
#####聚合函数
![示例](https://upload-images.jianshu.io/upload_images/9049859-8e3b927d10926065.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240)
???是吗
#####表生成函数
![示例](https://upload-images.jianshu.io/upload_images/9049859-296998517d5e21dd.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240)
#####其他内置函数
![示例](https://upload-images.jianshu.io/upload_images/9049859-b9eb2719d3b684d1.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240)
![示例](https://upload-images.jianshu.io/upload_images/9049859-76a11514adc93219.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240)
![示例](https://upload-images.jianshu.io/upload_images/9049859-0c4771f36b25d949.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240)
![示例](https://upload-images.jianshu.io/upload_images/9049859-e023d482e97aa594.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240)
![示例](https://upload-images.jianshu.io/upload_images/9049859-fcfd42d2256f33f4.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240)
##6. LIMIT语句
##7. 列别名
##8. 嵌套 SELECT 语句
##9. CASE WHEN THEN ESLE END 语句
##10. 什么情况下HIVE可以避免MAPREDUCE
1. 本地模式
2. where 过滤条件只是分区字段
3. 如果属性hive.exec.mode.local.auto的值设置为true的话, Hive还会尝试使用本地模式执行其他的操作
##11. WHERE 语句
##12. 谓词操作
##13. 关于浮点数的比较(☆)
![示例](https://upload-images.jianshu.io/upload_images/9049859-940a810c2c28f332.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240)
当用户写一个浮点数时,比如0.2, Hive会将该值保存为DOUBLE型的。我们之前定义deductions这个map的值的类型是FLOAT型的,这意味着Hive将隐式地将税收减免值转换为DOUBLE类型后再进行比较

为了简化一点,实际上我们可以说0.2对于FLOAT类型是0.2000001,而对于DOUBLE类型是0.20000000000这是因为一个8个字节的DOUBLE值具有更多的小数位(也就是小数点后的位数) 。当表中的FLOAT值通过Hive转换为DOUBLE值时,其产生的DOUBLE值是0.200000100000,0个值实际要比0.200000000001。这就是为什么这个查询结果像是使用了>=而不是>了

有两种规避的方法
1. 将底表的浮点数数据类型转化为double
2. cast(0.2 as float)

对浮点数进行比较时,需要保持极端谨慎的态度。要避免任何从窄类型隐式转换到更广泛类型的操作。

##14. LIKE 和 RLINKE
##15. GROUP BY 语句
##16. HAVING 语句
##17. JOIN 语句
###### 内连接
hive不支持非等值连接
#####JOIN 优化
多表连接,且有相同逻辑的连接词
小表放左边,hive也提供了一种标记机制
#####左外连接
#####右外连接
#####完全外连接
#####左半开连接
SEMI-JOIN比通常的INNER JOIN要更高效,原因如下:对于左表中一条指定的记录,在右边表中一旦找到匹配的记录, Hive就会立即停止扫描。从这点来看,左边表中选择的列是可以预测的。
#####笛卡尔积
#####map端join
##18. ORDER BY / SORT BY 
##19. 含有SORT BY的DISTRIBUTE BY
##20. CLUSTER BY
##21. 类型转换
##22. 抽样查询
##23. UNION ALL 


注意一下左半开连接和抽样查询
