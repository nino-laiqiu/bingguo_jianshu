1.数组
数组的特点

 	1. 数组中的元素类型必须一致
 	2. 数组本身是引用数据类型,但是里面的元素既可以是基本数据类型,也可以是引用数据类型
 	3. 数组的长度一旦给定无法改变
数组的动态初始化
数组的静态初始化

注意:空指针不能调用
equals

地址值问题

![image.png](https://upload-images.jianshu.io/upload_images/9049859-48a720ef59344697.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240)

数组最大值最小值的封装
![image.png](https://upload-images.jianshu.io/upload_images/9049859-2cfb8b7d5a4363e3.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240)
![image.png](https://upload-images.jianshu.io/upload_images/9049859-31957a352cd70a97.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240)

形式参数 和实际参数
形式参数：就是在定义函数或过程的时候命名的参数。通俗讲就是一个记号。2 实际参数：就是在执行时，调用函数或过程时，传递给函数或过程的参数。通俗讲就是实际值。3 参数嘛，就是一个可变量，它随着使用者的不同而发生变化。

数组的例题
![image.png](https://upload-images.jianshu.io/upload_images/9049859-b33ff1995d308e5a.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240)

##生成不重复的随机数
```
import java.util.ArrayList;
import java.util.Arrays;
import java.util.Enumeration;
import java.util.Random;

import static java.lang.System.*;

public class Demo6 {
    public static void main(String[] args) {
        int n =20;
        boolean[] array = new boolean[33];
        ArrayList<String> strings = new ArrayList<String>();
          int num =0;
        Random random = new Random();
        for (int i = 0; i < n; i++) {
            do {
                num = random.nextInt(33);
            }
            while (array[num]);
            array[num] =true;
             System.out.println(num);
            System.out.println(Arrays.toString(array));
        }
    }
}
```




