1.带包编译
自动式:

1. javac(空格) -d (空格).(空格) 文件名.java
2. java   包名.类名

2.权限修饰符

3.内部类
#成员内部类
1. 无限制的访问外部类中的成员,包括静态成员和私有成员

        2. 成员内部类中不能定义静态成员

        3. 如何创建内部类的对象:

                        **外部类名.内部类名  对象名 = new 外部类名().new 内部类名();**

        4. 如何在内部类中访问外部类中同名的成员  

                           外部类名.this.成员

 成员内部类: 没有static修饰,定义在成员变量的位置
	 *  1. 成员内部类可以无限制的访问外部类中的成员,包括静态成员和私有成员
	 *  2. 成员内部类中,不能定义静态的成员. 
	 *  3. 如何创建内部类的对象:
	  	**外部类名.内部类名  对象名 = new 外部类().new 内部类名();**
	 *  4. 如何在内部类中调用外部类中同名的成员:
	 *  		外部类名.this.成员


#局部内部类
**定义在方法中或者是代码块中的类**

            1. 局部内部类只能在方法中被认识

            2. 局部内部类不能使用public private protected 修饰符修饰

#静态内部类
**用static修饰的内部类**

         1. 静态内部类中即可以定义静态成员,也可以定义非静态的成员

         2. 有静态成员(变量和方法)的内部类,一定是静态内部类

         3. 如何访问静态内部类中的成员

                           静态成员:           外部类名.内部类名.静态成员

                           非静态的成员:    new 外部类名.内部类名().非静态成员



#匿名内部类

```
public class NoNameInnerClassDemo {
	public static void main(String[] args) {
		InterA a = new A();// 非匿名内部类的方式; 实现了InterA 接口     的子类的对象
		a.eat();
		//使用匿名内部类
		InterA b = new InterA() {
			@Override
			public void eat() {
				System.out.println("dddd");
			}
		};
		b.eat();
		Fly f = new Fly() {//使用变量接收匿名内部类
			@Override
			public void fly() {
				System.out.println(" i can fly, i am singing in the sky");
			}
		};
		f.fly();
		new Climb() {//使用匿名内部类直接调用方法
			public void climb() {
				System.out.println("没有比人更高的山");
			}
		}.climb();
	}
}
abstract class Fly{
	abstract public void fly();
}
class Climb{
	public void climb() {
		System.out.println("爬");
	}
}
interface InterA{
	public void eat();
}
class A implements InterA{
	@Override
	public void eat() {
		System.out.println("食之无味,弃之可惜");
	}
}
```
```
public class Demo7 {
    public static void main(String[] args) {
        Fu fu = new Fu() {
            @Override
            public void get() {
                //super.get();
            }

            @Override
            public void get1() {
                //super.get1();
            }
        };
        fu.get();
        fu.get1();
    }
}
class  Fu{
    public /*static*/ void get(){
        System.out.println("get");
    }
    public void  get1(){
        System.out.println("get1");
    }
}
```

4.JunitTest使用

5.理解多态
```
ublic class Demo10 {
    public static void main(String[] args) {
        Zi2 zi2 = new Zi2();
        zi2.op1();//父类特有方法
        zi2.get();//子类有 找子类的
        Fu2 fu2 = new Fu2();
        fu2.get();//直接找父类的
        Fu2 zi21 = new Zi2();//这是多态写法
        zi21.s ="s";
        zi21.op1();
        zi21.get();
        zi21.set1();
        Zi2 zi22 = (Zi2) zi21;
        zi22.s = "ss";
        zi22.op1();
        zi22.get();


    }
}
class  Fu2{
    String s;

    public void  op1(){
        System.out.println("父类特有方法");
        System.out.println(s);
    }
    public  void  get(){
        System.out.println("父类方法,子类不重写");
    }
    public void  set1(){
        System.out.println("共有方法");
    }
}
class  Zi2 extends  Fu2{
    String s;//父类子类一样的成员变量
    @Override
    public void set1() {
        System.out.println("共有方法重写子类");
    }


    public void get() {
        System.out.println("不重写的共有方法");
        System.out.println(s);
    }
    public void  op(){
        System.out.println("子类特有方法");
    }
}
/*父类特有方法
null
不重写的共有方法
null
父类方法,子类不重写
父类特有方法
s
不重写的共有方法
null
共有方法重写子类
父类特有方法
s
不重写的共有方法
ss*/
```




