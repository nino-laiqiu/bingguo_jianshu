#这个项目还没做完,把写了两天的代码粘贴如下:
![项目](https://upload-images.jianshu.io/upload_images/9049859-e4fcaeea41631943.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240)

1.还没有封装
2.第二次登陆,所有的信息都重置,而真实的是写入文本的,因此管理员内容不完善
3.采取了properties集合,配置文件,对于真实状况的描述不清,获取数据的杂乱
4.对于第二次登陆,目前所学有俩方法一种是读取切割不推荐,另外一种是采取序列化,但是要修改思路....懒得写 序列化采取另搞一个javabin  但是如果数据过多复杂....
5.管理员的数据获取是从配置文件获取的,因此如果配置文件的那个类没有加载就会报错,第二管理员数据获取的路径的固定的,因此程序不健壮,在管理员的方法那,我写了好几种方法都给注掉了,感觉都不是很好
```
import java.util.List;
//这是管理员
public class Administrator {
    private String nmaeId;
    private String password;
    private String situation;
    private List<Book> books;

    public Administrator() {
    }

    public Administrator(String nmaeId, String password) {
        this.nmaeId = nmaeId;
        this.password = password;
    }

    public Administrator(String nmaeId, String password, String situation, List<Book> books) {
        this.nmaeId = nmaeId;
        this.password = password;
        this.situation = situation;
        this.books = books;
    }

    public String getNmaeId() {
        return nmaeId;
    }

    public void setNmaeId(String nmaeId) {
        this.nmaeId = nmaeId;
    }

    public String getPassword() {
        return password;
    }

    public void setPassword(String password) {
        this.password = password;
    }

    public String getSituation() {
        return situation;
    }

    public void setSituation(String situation) {
        this.situation = situation;
    }

    public List<Book> getBooks() {
        return books;
    }

    public void setBooks(List<Book> books) {
        this.books = books;
    }

    @Override
    public String toString() {
        return "Administrator{" +
                "nmaeId='" + nmaeId + '\'' +
                ", password='" + password + '\'' +
                ", situation='" + situation + '\'' +
                ", books=" + books +
                '}';
    }
}
```
```
import java.io.*;
import java.util.*;
//可以把所有的信息都存入到文本里,管理员读取文本的数据
public class AdministratorOperatation {
       static   AdministratorOperatation administratorOperatation = new AdministratorOperatation();
        static Scanner scanner = Catalogue.scanner;
       static List<borrowLog>borrowandreturnLog = new ArrayList<>();
        //采取另一种方法是加载
    static {Log.log.showUserBRBooks();
            Log.log.readnotBorrow();}
        //获取在这个集合中存储的用户借阅情况 这个集合存储了borrowLog对象
        static List<borrowLog> stringborrowLog =  Log.log.stringborrowLog;
        static List<borrowLog> stringnotborrowLog =  Log.log.stringnotborrowLog;

     public void administratorBorrowAll(Administrator administrator){

           //查看所有用户的借阅信息  首先要判断是否为空则退出返回管理员子菜单
         if (!administrator.getSituation().equals("超级管理员")){
             System.out.println("功能只限超级管理员,退出到登录界面");
             Catalogue.catalogue.catalogue();
         }
         //不能这么搞 容易报错 采取读取日志的方法
        /* if (stringborrowLog.size()<=0) {
             System.out.println("用户没有借书,请继续其他操作,跳转回管理员子菜单");
             Catalogue.catalogue.administratoryemian();
         }
         else {
             //遍历LIST集合,迭代器
             Iterator<borrowLog> iterator = stringborrowLog.iterator();
             while (iterator.hasNext()) {
                 System.out.println(iterator.next());
             }
         }*/
        //IO流读取log
         String path = Log.log.p4;//如果首先调用则p3还没有加载........所以怎么设定
         try {
             BufferedReader bufferedReader = null;
             try {
                 bufferedReader = new BufferedReader(new FileReader(path));
             } catch (FileNotFoundException e) {
                 e.printStackTrace();
             }
             String str =null;
             while ((str=bufferedReader.readLine() )!= null) {
                 //如果没有用户操作则打印为空,为保证程序的健壮性要判断一下
                 System.out.println(str);
             }
         } catch (IOException e) {
             e.printStackTrace();
         }
         //返回管理员菜单
         Catalogue.catalogue.administratoryemian();

     }
    public void administratorBorrowThisUser(){
        /*if (stringborrowLog.size()<=0) {
            System.out.println("用户没有借书,请继续其他操作,跳转回管理员子菜单");
            Catalogue.catalogue.administratoryemian();
        }*/
            //某一用户的借阅情况 询问是哪一个用户  要判断这个用户是否存在
        System.out.println("请输入你想查询的用户(借阅情况)");
        String userNmaeNext = scanner.next();
        /*ListIterator<borrowLog> borrowLogListIterator = stringborrowLog.listIterator();
        while (borrowLogListIterator.hasNext()) {}*/
        //不能这么搞,读取日志
       /* for (borrowLog borrowLog : stringborrowLog) {
             if (userNmaeNext.equals(borrowLog.getUserId())){
                 //存在这个用户 取出这个用户的书籍情况
                 System.out.println(borrowLog);//是唯一的用户吗????
             }
             else {
                 System.out.println("没有这个用户---");
                 Catalogue.catalogue.administratoryemian();
             }
        }*/
        String path = Log.log.p4;//如果首先调用则p3还没有加载
        try {
            BufferedReader bufferedReader = null;
            try {
                bufferedReader = new BufferedReader(new FileReader(path));
            } catch (FileNotFoundException e) {
                e.printStackTrace();
            }
            String str =null;
            while ((str=bufferedReader.readLine() )!= null) {
                //如果没有用户操作则打印为空,为保证程序的健壮性要判断一下
                //System.out.println(str);
                String[] s = str.split(" ");
                //创建一个LIST集合存储未归还的书籍
                borrowandreturnLog.add(new borrowLog(s[0], s[1], s[2], s[3], s[4], s[5], s[6]));
                for (borrowLog borrowLog : borrowandreturnLog) {
                    if (userNmaeNext.equals(borrowLog.getUserId())){
                        System.out.println(borrowLog);
                    }
                }
            }
        } catch (IOException e) {
            e.printStackTrace();
        }
    }
    public void administratorBorrowBookNotReturn(Administrator administrator){
         //加载 把未归还的信息存放到集合中

         if (administrator.getSituation().equals("普通管理员")){
             System.out.println("该功能普通管理员不能访问,跳转到登录界面");
             Catalogue.catalogue.catalogue();
         }
        /*if (stringnotborrowLog.size()<=0) {
            System.out.println("用户没有借书,请继续其他操作,跳转回管理员子菜单");
            Catalogue.catalogue.administratoryemian();
        }*/
        //显示为还书籍
       // Log.log.readnotBorrow();
        //LIST迭代器遍历 显示为还书籍
       /* ListIterator<borrowLog> notborrowLogListIterator = stringnotborrowLog.listIterator();
        while (notborrowLogListIterator.hasNext()) {
            System.out.println(notborrowLogListIterator.next());
        }*/
        String path = Log.log.p3;
        try {
            BufferedReader bufferedReader = null;
            try {
                bufferedReader = new BufferedReader(new FileReader(path));
            } catch (FileNotFoundException e) {
                e.printStackTrace();
            }
            String str =null;
            while ((str=bufferedReader.readLine() )!= null) {
                //如果没有用户操作则打印为空,为保证程序的健壮性要判断一下
                System.out.println(str);
            }
        } catch (IOException e) {
            e.printStackTrace();
        }
        Catalogue.catalogue.administratoryemian();
    }
    public void administratorBorrowBookCanReturn(Administrator administrator){
           //读取日志或者读取文本
           // 获取p3读取log日志
           //怎么处理异常........难点不会
         String path = Log.log.p3;//如果首先调用则p3还没有加载........所以怎么设定
        try {
            BufferedReader bufferedReader = null;
            try {
                bufferedReader = new BufferedReader(new FileReader(path));
            } catch (FileNotFoundException e) {
                e.printStackTrace();
            }
            String str =null;
            while ((str=bufferedReader.readLine() )!= null) {
                //如果没有用户操作则打印为空,为保证程序的健壮性要判断一下
                System.out.println(str);
            }
        } catch (IOException e) {
            e.printStackTrace();
        }
    }
    public void administratorBooksSituation(){
         //要获取状态则要把原来的数据进行切割获取,所以采取调用方法的方式来获取状态
         //
        for (borrowLog borrowLog : stringborrowLog) {
            System.out.println(borrowLog.getBookSituation()+"  " +borrowLog.getBookNmae());
            //可以创建一个book对象 序列化 下一次更新数据库
        }
        Catalogue.catalogue.administratoryemian();
    }
}
```

```
public class Book {
    private String bookId;
    private String bookNmae;
    private int bookNumber;
    private String bookSituation;
    private long borrowTime;
    private long returnTime;

    @Override
    public String toString() {
        return
                "书的ID\t" + bookId + '\'' +
                "\t书的名字\t" + bookNmae + '\'' +
                "\t书的数量\t" + bookNumber + '\'' +
                "\t书的状态" + bookSituation + '\'' +
                "\t书籍借出时间\t" + borrowTime +
                "\t书籍归还时间\t" + returnTime
                ;
    }

    public String getBookId() {
        return bookId;
    }

    public void setBookId(String bookId) {
        this.bookId = bookId;
    }

    public String getBookNmae() {
        return bookNmae;
    }

    public void setBookNmae(String bookNmae) {
        this.bookNmae = bookNmae;
    }

    public int  getBookNumber() {
        return bookNumber;
    }

    public void setBookNumber(int  bookNumber) {
        this.bookNumber = bookNumber;
    }

    public String getBookSituation() {
        return bookSituation;
    }

    public void setBookSituation(String bookSituation) {
        this.bookSituation = bookSituation;
    }

    public long getBorrowTime() {
        return borrowTime;
    }

    public void setBorrowTime(long borrowTime) {
        this.borrowTime = borrowTime;
    }

    public long getReturnTime() {
        return returnTime;
    }

    public void setReturnTime(long returnTime) {
        this.returnTime = returnTime;
    }

    public Book(String bookId, String bookNmae, int  bookNumber, String bookSituation, long borrowTime, long returnTime) {
        this.bookId = bookId;
        this.bookNmae = bookNmae;
        this.bookNumber = bookNumber;
        this.bookSituation = bookSituation;
        this.borrowTime = borrowTime;
        this.returnTime = returnTime;
    }

    public Book() {
    }


}
```

```
import java.util.*;

public class BorrowBook {
    static BorrowBook borrowBook = new BorrowBook();
    static Scanner scanner = Catalogue.scanner;
    // static User user = Register.user;
    static Map<String, Book> bookList = DataBases.getBookList();
    //传递对副本进行操作
    static Map<String, Book> dataBooks = new HashMap<>(bookList);
    //借书的方法
    public void borrowBook(User user) {
        //显示书籍
        OperationBook.operationBook.showAllBooks();
        //询问要那本书
        System.out.println("请输入您要借阅书籍的ID");
        String bookId = scanner.next();
        if (dataBooks.containsKey(bookId)) {
            //获取书籍 判断书籍 从数据库中获取书的ID进行判断
            Book book = dataBooks.get(bookId);
            if (book.getBookNumber() > 0) {
                //这是重点
                //获取书籍  可以优化吗
                //在副本里操作 防止影响
                List<Book> userbooks = new ArrayList<Book>(user.getBooks());
                //   List<Book> userbooks = user.getBooks();
                long time = new Date().getTime();
                //添加     Map的特性.............
                Book borrowuserbook = new Book(book.getBookId(), book.getBookNmae(), 1, "借", time, 0);
                //添加到用户系统
                //对于用户类 不是map类所以采取这种方式  对于map集合采取覆盖操作
                //添加到用户类
                userbooks.add(borrowuserbook);
                //设置回去
                user.setBooks(userbooks);
                //埋点获取日志
                //......要注意代码的规范
                String log = Log.log.standardBorrowbooklog(user.getUserId(), borrowuserbook);
                try {
                   // Log.log.borrowAndRrturnbooklog(log, 1);
                    Log.log.borrowAndRrturnbooklog(log, 3);
                } catch (Exception e) {
                    e.printStackTrace();
                }

                //把书库给刷新一遍
                book.setBookNumber(book.getBookNumber() - 1);
                // 有没有更好的方法 刷新 如果第二次借相同的书TIME重置了
                dataBooks.put(book.getBookId(), book);
                DataBases.setBookList(dataBooks);
                System.out.println("借书成功,返回用户子页面");
                Catalogue.catalogue.login1();
            } else {
                System.out.println("书籍的库存不够,请重新借书");
                borrowBook(user);
            }
        } else {
            System.out.println("您输入的ID错误,请重新输入");
            borrowBook(user);
        }
    }

    //4 查看用户借阅书籍的情况
    public void showBorrowUserBooks(User user) {
//拿出用户的书籍 判断用户借书是否为空
        List<Book> books = user.getBooks();
        if (books.size() > 0) {
            System.out.println("借阅的书籍,如下");
            for (Book book : books) {
                //在这里进行对borrowBook.log日志的埋点
                String log = Log.log.standardBorrowbooklog(user.getUserId(), book);
                Log.log.borrowAndRrturnbooklog(log, 1);
                System.out.println(book.toString());
            }
            Catalogue.catalogue.login1();
        } else {
            System.out.println("您借阅的书籍为空,请回借阅");
            Catalogue.catalogue.login1();
        }

    }
    //判断用户书库是否为空,否则跳转到借书界面
    public  void useLogEmpty(User user){
        List<Book> books = user.getBooks();
        if (books.size()>0){
            //可以查看借阅日志
            Log.log.showUserBRBooks();
        }
        else {
            System.out.println("您还没借书,请前往借书");
            Catalogue.catalogue.login1();
        }
    }
}

```

```
public class borrowLog {
    private String userId;
    private String bookId;
    private String bookNmae;
    private String bookNumber;
    private String bookSituation;
    private String borrowTime;
    private String returnTime;

    @Override
    public String toString() {
        return "borrowLog{" +
                "userId='" + userId + '\'' +
                ", bookId='" + bookId + '\'' +
                ", bookNmae='" + bookNmae + '\'' +
                ", bookNumber='" + bookNumber + '\'' +
                ", bookSituation='" + bookSituation + '\'' +
                ", borrowTime='" + borrowTime + '\'' +
                ", returnTime='" + returnTime + '\'' +
                '}';
    }

    public String getUserId() {
        return userId;
    }

    public void setUserId(String userId) {
        this.userId = userId;
    }

    public String getBookId() {
        return bookId;
    }

    public void setBookId(String bookId) {
        this.bookId = bookId;
    }

    public String getBookNmae() {
        return bookNmae;
    }

    public void setBookNmae(String bookNmae) {
        this.bookNmae = bookNmae;
    }

    public String getBookNumber() {
        return bookNumber;
    }

    public void setBookNumber(String bookNumber) {
        this.bookNumber = bookNumber;
    }

    public String getBookSituation() {
        return bookSituation;
    }

    public void setBookSituation(String bookSituation) {
        this.bookSituation = bookSituation;
    }

    public String getBorrowTime() {
        return borrowTime;
    }

    public void setBorrowTime(String borrowTime) {
        this.borrowTime = borrowTime;
    }

    public String getReturnTime() {
        return returnTime;
    }

    public void setReturnTime(String returnTime) {
        this.returnTime = returnTime;
    }

    public borrowLog() {
    }

    public borrowLog(String userId, String bookId, String bookNmae, String bookNumber, String bookSituation, String borrowTime, String returnTime) {
        this.userId = userId;
        this.bookId = bookId;
        this.bookNmae = bookNmae;
        this.bookNumber = bookNumber;
        this.bookSituation = bookSituation;
        this.borrowTime = borrowTime;
        this.returnTime = returnTime;
    }
}


```


````
import java.io.IOException;
import java.util.*;

//主目录
public class Catalogue {
    static Catalogue catalogue =  new Catalogue();
    static Scanner scanner = new Scanner(System.in);
    static List<User> users = new ArrayList<User>();
    static Map<String, Book> bookList = DataBases.bookList;
    static Map<String, User> userHashMap = DataBases.userHashMap;
    static Map<String, Administrator> userHashMap1 = DataBases.userHashMap1;

    public static void catalogue()  {
        System.out.println("请输入数字选择\r\n1.用户登录\r\n2.用户注册\r\n3.管理员登录(此功能没有写入文本,必须加载其他类之后才能使用)\r\n4.退出系统\r\n");
        String bigSystem = scanner.next();
        switch (bigSystem) {
            case "1":
                // System.out.println("请输入您的账号");
                int count1 = 0;
                Register.register.login(count1);//登录
                // login1();//子类
                break;
            case "2":
                // System.out.println("请输入您的账号");
                int count = 0;
                Login.login.management(userHashMap, count);//账号的判断
                break;
            case "3":
                int count3 =0;
                Register.register.administratoRegisret(count3);
                break;
            case "4":
                System.out.println("退出系统再见,欢迎光临");
                System.exit(0);
                break;
            default:
                System.out.println("您输入的数字有误,请重新输入");
                catalogue();
        }
    }
    //用户的子页面
    public void login1() {
        System.out.println("请输入登录选项\r\n1.借书\r\n2.还书\r\n3.查看个人借阅记录\r\n4.查看个人借阅情况\r\n5.管理员查看");
        String next2 = scanner.next();
        switch (next2) {
            case "1":
                //register.user  为什么不用这个或者 Register.register.user  因为是在这个类里面
                BorrowBook.borrowBook.borrowBook(Register.user);
                break;
            case "2":
                ReturnBooks.returnBooks.returnBooks(Register.user);
                break;
            case "3":
                //IO流读取用户所有的借阅记录
                //判断用户书籍是否为空,如果为空跳转借书界面
                BorrowBook.borrowBook.useLogEmpty(Register.user);
                break;
            case "4":
                BorrowBook.borrowBook.showBorrowUserBooks(Register.user);
                // login1(this.user);???? login1(user);
                break;
            case "5":
                int count3 =0;
                 Register.register.administratoRegisret(count3);
                break;
            default:
                System.out.println("回归上一菜单");
                Catalogue.catalogue.catalogue();

        }
    }
    //管理员的子页面
    public   void   administratoryemian(){
        System.out.println("请输入管理员登录选项\r\n1.管理员查看所有人借阅信息\r\n" +
                "2.管理员查看指定人员信息\r\n3.管理员查看所有未归还书籍信息\r\n4.管理" +
                "员查看所有已归还书籍信息\r\n5.管理员查看所有书籍状态\r\n6.注册新管理员\r\n");
        String administratornext = scanner.next();
        switch (administratornext){
            case "1":
                AdministratorOperatation.administratorOperatation.administratorBorrowAll(Register.administrator);
                break;
            case "2":
                AdministratorOperatation.administratorOperatation.administratorBorrowThisUser();
                break;
            case "3":
                AdministratorOperatation.administratorOperatation.administratorBorrowBookNotReturn(Register.administrator);
                break;
            case "4":
                AdministratorOperatation.administratorOperatation.administratorBorrowBookCanReturn(Register.administrator);
                //
                break;
            case "5":
                AdministratorOperatation.administratorOperatation.administratorBooksSituation();
                break;
            case "6":
                int count =0;
                Login.login.administratorLog(Register.administrator,count);
                break;
            default:
                System.out.println("输入错误,重写----");
                administratoryemian();
        }
    }
}

```
```
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Map;

class DataBases {
    static Map<String, User> userHashMap = new HashMap<>();
    static Map<String, Administrator> userHashMap1 = new HashMap<>();
    static Map<String, Book> bookList = new HashMap<>();

    static User user =new User();
    static {
        getUser();
        getadministrator();
        getbook();
    }
    //用户数据库
    public static void getUser() {
        User u1 = new User("张三", "123456", new ArrayList<Book>());
        User u2 = new User("李四", "123456", new ArrayList<Book>());
        User u3 = new User("王五", "123456", new ArrayList<Book>());
        // users.add(new User("张三", "123456", new ArrayList<Book>()));
        userHashMap.put(u1.getUserId(), u1);
        userHashMap.put(u2.getUserId(), u2);
        userHashMap.put(u2.getUserId(), u3);
    }

    //管理员数据库
    public static void getadministrator() {
        Administrator superadministrator = new Administrator("1001", "123456", "超级管理员", new ArrayList<Book>());
        Administrator gaoadministrator = new Administrator("10011", "123456", "高级管理员", new ArrayList<Book>());
        Administrator yibanadministrator = new Administrator("100111", "123456", "普通管理员", new ArrayList<Book>());
        userHashMap1.put(superadministrator.getNmaeId(), superadministrator);
        userHashMap1.put(gaoadministrator.getNmaeId(), gaoadministrator);
        userHashMap1.put(gaoadministrator.getNmaeId(), yibanadministrator);
    }

    //图书馆数据库
    public static void getbook() {
        Book book1 = new Book("10001","貂蝉",20,"在库",0,0);
        Book book2 = new Book("10002","吕布",30,"在库",0,0);
        Book book3 = new Book("10003","杨玉环",2,"在库",0,0);
        Book book4 = new Book("10004","曹操",3,"在库",0,0);
        Book book5 = new Book("10005","关羽",90,"在库",0,0);
        bookList.put(book1.getBookId(), book1);
        bookList.put(book2.getBookId(), book2);
        bookList.put(book3.getBookId(), book3);
        bookList.put(book4.getBookId(), book4);
        bookList.put(book5.getBookId(), book5);

    }

    public static Map<String, User> getUserHashMap() {
        return userHashMap;
    }

    public static void setUserHashMap(Map<String, User> userHashMap) {
        DataBases.userHashMap = userHashMap;
    }

    public static Map<String, Administrator> getUserHashMap1() {
        return userHashMap1;
    }

    public static void setUserHashMap1(Map<String, Administrator> userHashMap1) {
        DataBases.userHashMap1 = userHashMap1;
    }

    public static Map<String, Book> getBookList() {
        return bookList;
    }

    public static void setBookList(Map<String, Book> bookList) {
        DataBases.bookList = bookList;
    }
}

```
```
import java.io.*;
import java.util.*;

//日志类
public class Log implements Serializable {
    //实现序列化
    private static final long serialVersionUID = 4088506530017170576L;
    //对配置文件的处理,及存储路径
    static Log log = new Log();
    //存放用户的借阅情况
    static List<borrowLog> stringborrowLog = new ArrayList<>();
    //存放未归还的书籍
    static List<borrowLog> stringnotborrowLog = new ArrayList<>();
    //为什么把p2 p3 p4 设置为成员变量 因为常用其他方法可调用  而p1不常用
     static String p2;
     static String p3;
     static String p4;
     static Properties properties = new Properties();
    //配置文件 日志的创建
    //怎么抛出异常
    public void useBorrowLog(String userId) throws Exception {
        //好像有问题.....
         properties.load(Thread.currentThread().getContextClassLoader().getResourceAsStream("src/Data/log.properties"));
        //为什么要设置获取path 因为如果把 String p1 给设置死了不符合程序的健壮性 此路径在log.properties配置文件下
        //对于配置文件的设置 .......生疏要复习一下
        String path = properties.getProperty("path");
        String p1 = path + userId;
        //借书日志
        p2 = p1 + "\\borrowBook.log";
        //还书日志
        p3 = p1 + "\\returnBook.log";
        //借书还书总和日志
        p4 = p1 + "\\userBookslog.log";
        //没有获取书库系统的借阅记录  如何获取
        //分别为这些路径设置File包装 并且要判断是否存在这些路径
        File file1 = new File(p1);
        File file2 = new File(p2);
        File file3 = new File(p3);
        File file4 = new File(p4);
        //判断这些包装  要保证p1是目录 其他是文件
        if (file1.exists()) {
            if (!file1.isDirectory()) {
                file1.mkdirs();
            }
        } else {
            file1.mkdirs();
        }
        if (!file2.exists()) {
            file2.createNewFile();
        }
        if (!file3.exists()) {
            file3.createNewFile();
        }
        if (!file4.exists()) {
            file4.createNewFile();
        }
    }

    //
    //下面两个是核心代码  保证读取的规范性 防止错误
    //规范borrow日志的记录
    public String standardBorrowbooklog(String userId, Book book) {
        return userId + " " + book.getBookId() + " " + book.getBookNmae() + " " + book.getBookSituation() + " " + book.getBookNumber()
                + " " + book.getBorrowTime() + " " + book.getReturnTime();
    }

    //规范Return日志的记录
    public String standardReturnbooklog(String userId, Book book) {
        return userId + " " + book.getBookId() + " " + book.getBookNmae() + " " + book.getBookSituation() + " " + book.getBookNumber()
                + " " + book.getBorrowTime() + " " + book.getReturnTime();
    }

    //记录借书和还书的操作
    public void borrowAndRrturnbooklog(String log, int flog) {
        //BufferedOutputStream bufferedOutputStream = new BufferedOutputStream(new ObjectOutputStream(new FileOutputStream(p2, true)));
        //拿到数据进行写入
        String path = " ";
        if (flog == 1) {
            path = p2;
        } else if (flog == 2) {
            path = p3;
        } else if (flog == 3) {
            path = p4;
        } else {
            System.out.println("系统错误");
            System.exit(0);
        }
        /*try {
            ObjectOutputStream objectOutputStream = new ObjectOutputStream(new FileOutputStream(path, true));
            objectOutputStream.writeObject(log);
            objectOutputStream.flush();
            //垃圾回收
            objectOutputStream=null;
            objectOutputStream.close();

        } catch (IOException e) {
            e.printStackTrace();
        }*/
        try {
            BufferedWriter bufferedWriter = new BufferedWriter(new FileWriter(path, true));
            bufferedWriter.write(log);
            bufferedWriter.flush();
            bufferedWriter.newLine();
            //注意顺序.....
            bufferedWriter.close();
            bufferedWriter = null;
        } catch (IOException e) {
            e.printStackTrace();
        }

    }

    //从借书日志中读取并且放在Map集合中或者List中
    public void readBorrow() {
        //这是整个借书和还书的方法
        String path = p4;
        try {
            BufferedReader bufferedReader = new BufferedReader(new FileReader(path));
            String log = null;
            while ((log = bufferedReader.readLine()) != null) {
                //考虑到日志的读取 把这些更正放在MAP(borrowLog)集合中 创建一个borrowLog
                String[] s = log.split(" ");
                  /*String userId = s[0];
                  String bookId =s[1];
                  String bookNmae=s[2];
                  String bookNumber=s[3];
                  String bookSituation=s[4];
                  String borrowTime=s[5];
                  String returnTime=s[6];*/
                stringborrowLog.add(new borrowLog(s[0], s[1], s[2], s[3], s[4], s[5], s[6]));
            }
            bufferedReader = null;
            //为什么不能关流 核心问题.........

        } catch (IOException e) {
            e.printStackTrace();
        }
    }
    //查看所有未归还书籍 把未归还的书籍从配置文件中读取
    public void readnotBorrow() {
        String path = p2;
        try {
            BufferedReader bufferedReader = new BufferedReader(new FileReader(path));
            String log = null;
            while ((log = bufferedReader.readLine()) != null) {
                String[] s = log.split(" ");
                //创建一个LIST集合存储未归还的书籍
                stringnotborrowLog.add(new borrowLog(s[0], s[1], s[2], s[3], s[4], s[5], s[6]));
            }
        } catch (IOException e) {
            e.printStackTrace();
        }

    }

    //展示用户借阅记录
    public void showUserBRBooks() {
        //展示借阅记录
        readBorrow();
        //遍历存储借阅激励的日志MAP
        //对MAP集合遍历的复习不允许相同的KEY
        //对LIST集合遍历,展示 有四种遍历方式:迭代器 LIST的迭代器 转数组 增强for循环 普通for循环
        for (borrowLog borrowLog : stringborrowLog) {
            System.out.println(borrowLog);
        }
        //.....本来采取MAP的但是MAP
        //方法一
       /* Collection<borrowLog> values = stringborrowLog.values();
        for (borrowLog value : values) {
            System.out.println(value);
        }*/
        //方法二 由指定键来获取value值
        // stringborrowLog.get("");
        //方法三 返回set的视图
       /* Set<Map.Entry<String, borrowLog>> entries = stringborrowLog.entrySet();
        for (Map.Entry<String, borrowLog> entry : entries) {
            entry.getKey();
            entry.getValue();}*/
        //方法四 使用迭代器 比较与ArrayList的两种迭代器遍历方法MAP集合只一种
       /* Set<Map.Entry<String, borrowLog>> entries1 = stringborrowLog.entrySet();
        Iterator<Map.Entry<String, borrowLog>> iterator = entries1.iterator();
        while (iterator.hasNext()) {
            iterator.next();}*/
        //返回用户子界面
        Catalogue.catalogue.login1();
    }
    //展示用户未还书籍
    //管理员可以调用这个方法来显示为还书籍
    public void showUserNotRorrowBooks(){readnotBorrow();
        for (borrowLog borrowLog : stringnotborrowLog) {
            System.out.println(borrowLog);
        }
    }
}
```
```
import java.util.ArrayList;
import java.util.Map;
import java.util.Scanner;

//注册模块
public class Login {
    static Scanner scanner = Catalogue.scanner;
    static Login login = new Login();
    static Map<String, Administrator> userHashMap1 = DataBases.userHashMap1;

    //用户的注册
    public static void management(Map<String, User> userHashMap, int count) {
        //判断账号和密码
        //判断次数
        count++;
        if (count > 5) {
            System.out.println("输入次数超过5次,再见-");
            System.exit(0);
        }
        System.out.println("请输入您的账号");
        String next = scanner.next();
        if (userHashMap.containsKey(next)) {
            System.out.println("账号存在,请重新输入,还剩 " + (5 - count) + "次");

            management(userHashMap, count);
        }
        System.out.println("请输入密码");
        String next1 = scanner.next();
        System.out.println("输入成功");
        userHashMap.put(next, new User(next, next1, new ArrayList<Book>()));
        Catalogue.catalogue.catalogue();

    }

    //管理员的注册,在管理员登录后注册
    public void administratorLog(Administrator administrator, int count) {
        if (count++ >= 5) {
            System.out.println("输入的次数超过了5次,退出系统");
            System.exit(0);
        }
        /*超级管理员可注册高级管理员、一般管理员
                而高级管理员只能注册一般管理员
        一般管理员无法注册新管理员
        同时每个非超级管理员都应该有直系上级进行关联，谁注册的，谁就是上级*/
        if (administrator.getSituation().equals("普通管理员")) {
            System.out.println("普通管理员无法注册----");
            System.exit(0);
        }
        if (administrator.getSituation().equals("高级管理员")) {
            System.out.println("可以注册普通管理员,请输入账号");
            String nextId = scanner.next();
            if (userHashMap1.containsKey(nextId)) {
                System.out.println("管理员账号存在,重新输入还有" + (5 - count) + "机会");
                administratorLog(administrator, count);
            } else {
                System.out.println("请输入你的密码");
                String nextPassWord = scanner.next();
                System.out.println("请输入您的注册类型");
                String nextSituation = scanner.next();
                if (nextSituation.equals("超级管理员")) {
                    System.out.println("该类型不可注册,请重新注册,你还有" + (5 - count) + "次机会");
                    administratorLog(administrator, count);
                }
                else if (nextSituation.equals("高级管理员")){
                userHashMap1.put(nextId,new Administrator(nextId,nextPassWord,nextSituation,new ArrayList<Book>()));
                }
                else {
                    System.out.println("账号类型错误,还有"+(5-count)+"机会");
                    administratorLog(administrator, count);
                }
            }
        }
        else {
            System.out.println("可以注册三种管理员,请输入账号");
            String nextId = scanner.next();
            if (userHashMap1.containsKey(nextId)) {
                System.out.println("管理员账号存在,重新输入还有" + (5 - count) + "机会");
                administratorLog(administrator, count);
            } else {
                System.out.println("请输入你的密码");
                String nextPassWord = scanner.next();
                System.out.println("请输入您的注册类型");
                String nextSituation = scanner.next();
                if (!nextSituation.equals("普通管理员")||!nextSituation.equals("超级管理员")||!nextSituation.equals("高级管理员"))
                {
                    System.out.println("账号类型错误,重写输入,还有"+(5-count)+"机会");
                    administratorLog(administrator, count);
                }
                else {
                userHashMap1.put(nextId,new Administrator(nextId,nextPassWord,nextSituation,new ArrayList<Book>()));
                }
            }
        }
    }
}

```
```
import java.util.Collection;
import java.util.Map;

//操作书
public class OperationBook {
       static OperationBook operationBook = new OperationBook();
       static Map<String, Book> bookList = DataBases.bookList;
public void showAllBooks(){
    System.out.println("书籍的列表如下");
    Collection<Book> values = bookList.values();
    for (Book value : values) {
        System.out.println(value.toString());
    }
}
}

```
```
ublic class PersistenceUserAndBook {

       static PersistenceUserAndBook persistence = new PersistenceUserAndBook();
       public  void  persistence(){
           //IO流写入读取过程,数据信息的可持续化,采取什么方式可以简化步骤

       }

}

```
```
import java.util.Map;
import java.util.Scanner;

//登录模块
public class Register {
    static Register register = new Register();
    //.......核心   跨类传输作用
    public static User user = new User();//......传递作用
    public static Administrator administrator = new Administrator();
    static Scanner scanner = Catalogue.scanner;
    static Map<String, Book> bookList = DataBases.bookList;
    static Map<String, User> userHashMap = DataBases.userHashMap;
    static Map<String, Administrator> userHashMap1 = DataBases.userHashMap1;

    public static void interfaceLibrary() {
        System.out.println("********************************\r\n" +
                "**    欢迎来到图书管理系统     **\r\n" +
                "**    故障电话 B:123456789    **\r\n" +
                "****   ******************* ****\r\n");
    }



    //用户登录
    public void login(int count2) {
        count2++;
        if (count2 > 5) {
            System.out.println("输入次数超过5次,再见-");
            System.exit(0);
        }
        System.out.println("登录请输入账号");
        String nextId = scanner.next();
        if (!userHashMap.containsKey(nextId)) {
            System.out.println("账号输入错误,还剩" + (5 - count2) + "次" + "请重新输入");
            login(count2);
        }
        System.out.println("请输入密码");
        String next1 = scanner.next();
        //怎么判断密码
        if (!userHashMap.get(nextId).getPassWord().equals(next1)) {
            System.out.println("密码输入错误,还剩" + (5 - count2) + "次机会");
            login(count2);
        }
        System.out.println("登录成功");
        User user = userHashMap.get(nextId);
        this.user = user;
        //判断用户是否建立了日志
        try {
            Log.log.useBorrowLog(nextId);
        } catch (Exception e) {
            //对异常的操作
            System.err.println("日记系统创建失败\r\n系统错误,退出系统");
            System.exit(-1);
        }
        Catalogue.catalogue.login1();
    }

    //管理员登录
    public  void administratoRegisret( int count3) {
        count3++;
        if (count3 > 5) {
            System.out.println("输入次数超过5次,再见-");
            System.exit(0);
        }
        System.out.println("登录请输入账号");
        String next = scanner.next();
        if (!userHashMap1.containsKey(next)) {
            System.out.println("账号输入错误,还剩" + (5 - count3) + "次" + "请重新输入");
            administratoRegisret(count3);
        }
        System.out.println("请输入密码");
        String next1 = scanner.next();
        //怎么判断密码
        if (!userHashMap1.get(next).getPassword().equals(next1)) {
            System.out.println("密码输入错误,还剩" + (5 - count3) + "次机会");
            administratoRegisret( count3);
        }
        System.out.println("登录成功");
        Administrator administrator = userHashMap1.get(next);
        this.administrator=administrator;
        //跳转到管理员的子页面
        Catalogue.catalogue.administratoryemian();


    }


}
```

```
import java.util.*;

public class ReturnBooks {
    static ReturnBooks returnBooks = new ReturnBooks();
    static Scanner scanner = Catalogue.scanner;
    static Map<String, Book> bookList = DataBases.bookList;
    static Map<String, Book> dataBooks = new HashMap<>(bookList);
    public void returnBooks(User user) {
        //获取用户的书籍库
        List<Book> booksUser = user.getBooks();
        if (booksUser.size() > 0) {
            //展示书籍
            System.out.println("展示书籍");
            OperationBook.operationBook.showAllBooks();
            System.out.println("请输入您要还书的ID");
            String booksId = scanner.next();
            //对副本操作,遍历副本寻找用户输入的ID,如果ID不正确 则继续借阅
            List<Book> book1s = new ArrayList<>(booksUser);
            for (Book book : book1s) {
                //对副本进行遍历  对原来数据进行操作 如果只对同一对象操作会引发多线程错误
                if (booksId.equals(book.getBookId())) {
                    //继续对副本遍历
                    //对用户书籍进行操作
                    Date date = new Date();
                    long time = date.getTime();
                    book.setBookSituation("还");
                    book.setReturnTime(time);
                    //对原来用户数据库继续操作
                    booksUser.remove(book);
                    //埋点获取日志记录
                    //对获取的清洗
                    String log = Log.log.standardReturnbooklog(user.getUserId(), book);
                    Log.log.borrowAndRrturnbooklog(log, 2);
                    Log.log.borrowAndRrturnbooklog(log, 3);
                    user.setBooks(booksUser);
                    //对数据库进行操作,获取KEY
                    Book bookData = dataBooks.get(booksId);
                    //进行加一
                    //难点是副本???? 分别对用户和数据库的操作  get set 方法
                    bookData.setReturnTime(time);
                    bookData.setBookNumber(bookData.getBookNumber() + 1);
                    bookData.setBookSituation("还");
                    bookList.put(booksId, bookData);
                    System.out.println("归还成功----");
                    Catalogue.catalogue.login1();
                    //这里是为什么只能依次还书的原因,每次判断都只能判断第一个循环
                } /*else { System.out.println("您输入的ID不正确,重新输入");
                    Catalogue.catalogue.login1();}*/
                }
            //else在哪放是一个难点....放的位置不同影响结果
                //不存在这个ID 重新选择
                System.out.println("您输入的ID不正确,重新输入");
                Catalogue.catalogue.login1();

        } else {
            System.out.println("你还没借过书,前往借书");
            BorrowBook.borrowBook.borrowBook(user);
        }

    }
}

```
```
import java.util.*;
public class testLibrary {
    static {
     Register.register.interfaceLibrary();
    }
    //测试类
    public static void main(String[] args) {
        Catalogue.catalogue.catalogue();
        //考虑到持久性把借阅的所有信息都读入到文本,这样管理员可以直接登录查看借阅信息了;
        //还没写
        PersistenceUserAndBook.persistence.persistence();
    }
}

```
```
import java.util.List;

public class User {
    private String userId;//ID
    private String passWord;//密码
    private String stuation;//状态
    private String bookNumber;//借阅数量
    private List<Book> books;//借阅书籍有哪些

    public User(String userId, String passWord, List<Book> books) {
        this.userId = userId;
        this.passWord = passWord;
        this.books = books;
    }

    public User() {
    }

    public User(String userId, String passWord, String stuation, String bookNumber, List<Book> books) {
        this.userId = userId;
        this.passWord = passWord;
        this.stuation = stuation;
        this.bookNumber = bookNumber;
        this.books = books;
    }

    public String getUserId() {
        return userId;
    }

    public void setUserId(String userId) {
        this.userId = userId;
    }

    public String getPassWord() {
        return passWord;
    }

    public void setPassWord(String passWord) {
        this.passWord = passWord;
    }

    public String getStuation() {
        return stuation;
    }

    public void setStuation(String stuation) {
        this.stuation = stuation;
    }

    public String getBookNumber() {
        return bookNumber;
    }

    public void setBookNumber(String bookNumber) {
        this.bookNumber = bookNumber;
    }

    public List<Book> getBooks() {
        return books;
    }

    public void setBooks(List<Book> books) {
        this.books = books;
    }

    public User(String userId, String passWord) {
        this.userId = userId;
        this.passWord = passWord;

    }

    @Override
    public String toString() {
        return "User{" +
                "userId='" + userId + '\'' +
                ", passWord='" + passWord + '\'' +
                ", stuation='" + stuation + '\'' +
                ", bookNumber='" + bookNumber + '\'' +
                ", books=" + books +
                '}';
    }
}

```
